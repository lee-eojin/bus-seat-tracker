# Findings — 서비스화 사전 조사 (2026-07-27)

## 0. API 호출 예산 — 이 프로젝트의 구속 제약

**일 한도 1,000회** (2026-07-27 확인). 수집 주기, 노선 추가, 실시간 기능은 전부 이 예산 안에서만 가능하다.
**기능을 설계하기 전에 호출 예산부터 계산한다.**

호출은 서비스 경로가 갈린다 (`bus-seat-collector/collector.ts:12-18`).

| 서비스 | 엔드포인트 | 쓰는 곳 |
|---|---|---|
| `busrouteservice` | `getBusRouteListv2`, `getBusRouteStationListv2` | 노선 ID·정류장 목록 |
| `buslocationservice` | `getBusLocationListv2` | **좌석 관측 — 서비스 실시간이 쓸 것** |

> **미확인**: 1,000회가 서비스별 집계인지 계정 전체 합산인지 확정하지 못했다.
> 이게 갈리면 가용 예산이 두 배 차이 난다. 서비스 설계의 선결 조건.

### 소모량 (노선 2개 = 3330, 1650)

| 항목 | 값 | 근거 |
|---|---|---|
| 수집 사이클당 `buslocationservice` | 2회 | 노선당 1회 (`collector.ts:collectOnce`) |
| 수집 사이클당 `busrouteservice` | 2회 → **캐시 미스 시에만** | 아래 수정 참조 |
| 하루 사이클 수 | 약 237 *(추정)* | 조밀 60초 구간이 대부분 (아침 129 + 저녁 99 + 정시 약 9) |
| `buslocationservice` 일 소모 | 약 474회 | 237 × 2 |
| **서비스 가용분** | **약 526회** | 서비스별 집계일 경우 |

### 해결한 낭비 (2026-07-27)

수집기가 `--once` 실행마다 노선 ID와 정류장 목록을 다시 받아 `busrouteservice`를
**하루 약 948회 — 한도의 95%** 태우고 있었다. `resolveRoutes()` 캐시로 **948 → 4회/일**.

- 기본 24시간 TTL, `ROUTE_CACHE_HOURS`로 조정, `--refresh-routes`로 강제 갱신
- 캐시 미스일 때만 파일을 쓰므로 `routes/*-stops.json`의 사이클별 재기록·커밋도 사라짐
- 검증 6/6 통과 — 재사용 로그 + 캐시 파일 mtime 불변 두 가지로 판정

**노선을 하나만 더 늘려도 예산이 비례해 늘어난다.** `ROUTE_NAMES` 변경 전 재계산할 것.
한도를 넘기면 `findRoute`가 실패하며 수집 전체가 멈추고, 백테스트 표본에 구멍이 난다.

---

## 1. 지금 있는 것

### 파이프라인 (전부 GitHub Actions 위에서 돈다)
```
GBIS API ──(collect-bus-seats.yml)──> bus-seat-tracker-data (private repo, JSONL)
                                          │
                                          ├──(publish-pages.yml, 30분)──> build-data.ts
                                          │                                  ↓
                                          │                        data/{latest,history,daily,profile}.js
                                          │                                  ↓
                                          └──> GitHub Pages 정적 사이트 (prototype-bus/)
```

- 수집: 평일 피크(07:00\~09:00, 18:00\~19:30) 60초, 피크 창(06:30\~10:00, 17:30\~20:30) 10분, 그 외 운행시간 매시, 심야 휴지.
- 저장: git 브랜치 `collect/YYYY-MM-DD`에 누적 → 다음날 main 단일 아카이브 커밋.
- 발행: 매시 17분·47분. **화면 신선도 하한이 30분**이라 실시간이 아니다.
- 실시간 오버레이는 브라우저가 GBIS를 직접 호출 — `data/config.js`에 키를 넣는 방식이라 **공개 배포 시 키 노출**. README에도 "그때는 프록시가 필요"라고 이미 적혀 있다.

### 코드 자산 (5,068줄)
| 파일 | 줄 | 역할 |
|---|---|---|
| `prototype-bus/app.ts` | 1,050 | 노선 축 UI, 좌석 DP 전파, 추천, 네이버지도 길찾기, Phase 0 로그 |
| `backtest/queue-recovery.ts` | 449 | 대기 인원 복원 (누적곡선·λ 추정) |
| `shared/profile.ts` | 387 | 순수요 프로파일, 구간합 역산(ridge 좌표하강) |
| `backtest/backtest.ts` | 363 | rolling-origin 백테스트 |
| `shared/model.ts` | 352 | 도메인 모델 |
| `prototype-bus/build-data.ts` | 364 | JSONL → 브라우저 데이터 |
| `bus-seat-collector/collector.ts` | 251 | GBIS 수집기 |

### 검증된 모델 성과 (docs/05-validation-2026-07-24.md)
- 도착 좌석 MAE **4.34석** (naive-persist 6.91 대비 우세, 모든 horizon)
- 만석 Brier **0.046** (v1 만석빈도 0.070, naive 0.083 대비 우세)
- 예측구간 포함률 74.9% (목표 80% 미달 — 구간이 좁다)
- **+1 정류장 귀속 오프셋 실증**: API 잔여석이 "도착 시점(승차 반영 전)" 값임을 필드 실측으로 확정
- 남은 편향: 만석 확률 과소예측(under-confident), u_day·headway 층 미구현

### 아직 안 되는 것 (docs/04-queue-recovery.md §7, §14)
- **대기 인원 예측기는 아직 못 씀**. λ 날짜 간 분산 미측정(조밀 데이터 1일치). 사후 재구성만 가능.
- 혼잡 구간 하한이 실측 대비 최대 4배 느슨함.
- 중간 투입 차량(§13)을 모델이 못 봄.
- 층-2(사용자 경쟁 수요)는 데이터 자체가 없음 — localStorage 기록뿐.

## 2. 서비스화 관점의 갭

| # | 갭 | 현재 | 서비스에 필요한 것 | 난이도 |
|---|---|---|---|---|
| G1 | **API 키 노출** | 클라이언트가 직접 GBIS 호출 | 서버 프록시 (키 서버 보관) | 낮음, 필수 |
| G2 | **실시간성** | 30분 주기 정적 발행 | 요청 시점 조회 또는 짧은 캐시 | 중간, 필수 |
| G3 | **저장소** | git repo에 JSONL 누적 | DB (시계열 조회·집계) | 중간 |
| G4 | **노선 확장** | 3330, 1650 하드코딩(`ROUTE_NAMES`) | 임의 노선 온디맨드 | 중간 |
| G5 | **사용자 데이터** | localStorage (기기 안에만) | 서버 수집 → 층-2 학습 루프 | 중간 |
| G6 | **법·정책** | 없음 | 공공데이터 출처 표기, 개인정보 처리방침, 이용약관, 면책 | 낮음, 필수 |
| G7 | **관측성** | Actions 로그만 | 에러 추적, 예보 정확도 대시보드 | 낮음 |
| G8 | **인증** | 없음 | 즐겨찾기·기록 저장하려면 필요 (익명 ID로 시작 가능) | 낮음 |

## 3. 서비스로서 팔 수 있는 핵심 가치

경쟁 서비스(카카오맵·네이버지도·경기버스정보)가 **못 하는 것**:

1. **도착 시점 좌석 예보** — 기존 앱은 "현재 잔여석"만 보여준다. 이 프로젝트는 "그 버스가 내 정류장에 올 때 몇 석일지"를 낸다. 백테스트로 검증됨.
2. **어느 정류장으로 갈지** — 만차 전에 탈 수 있는 상류 정류장을 노선 축 위에서 제시.
3. **정직한 불확실성** — 표본 부족 구간은 예측 대신 보류(`*` 표시, 하한만 제시).

이게 서비스의 차별점이고, 이미 동작한다. **서비스화는 새 모델을 만드는 일이 아니라 배포 형태를 바꾸는 일이다.**

## 4. 제약

- 데이터 경계: 원본 차량번호(plateNo)는 표시·저장 금지. 가명화는 HMAC, 시크릿은 API 키와 분리.
- git commit/push/merge는 사용자만 실행.
- 공개 저장소에 API 키·원문 차량번호·수집 데이터·빌드 산출물 금지.
- GBIS API는 공공데이터포털 일 호출 한도가 있다 — 서버 프록시로 전환하면 사용자 수 × 폴링 주기가 한도에 걸린다. **캐시 계층이 선택이 아니라 필수.**

## 5. GA4 기본 계측 (2026-07-29)

- 배포 빌드는 `prototype-bus/index.html`과 `legal.html`을 그대로 `site/prototype-bus/`에 복사한다.
  따라서 두 원본 문서에 같은 Google 태그를 넣어야 배포 페이지 전체가 계측된다.
- 기존 Google 태그는 없었다. 측정 ID는 `G-QFFRQ5Z4XX`.
- 기존 개인정보 안내는 서버 저장이 없다고 적혀 있었지만, 실제 코드는 무작위 방문자 ID와 방문 기록,
  설문 응답을 `/api/feedback`으로 전송한다. GA4 도입과 함께 실제 처리 항목을 기준으로 문구를 수정했다.
