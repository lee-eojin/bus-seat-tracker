# 구조·문서 감사 결과

## 실행 구조

현재 저장소는 실행 책임보다 개발 순서에 가까운 이름을 사용한다. `prototype-bus`, `shared`, `server`, `scripts`, `backtest`만 보고는 어떤 코드가 제품이고 어떤 코드가 연구 도구인지 알기 어렵다.

확정한 목표 구조는 다음과 같다.

```text
apps/                 사용자가 직접 만나는 실행 애플리케이션
  web/                정적 화면과 데이터 번들 생성
  api/                HTTP 요청 처리와 외부 서비스 연동
api/                  Vercel 파일시스템 라우트 어댑터
packages/
  domain/             좌석·수요·대기열 도메인 규칙
tools/
  collector/          GBIS 수집기와 수집 스케줄
research/
  backtest/           모델 검증과 대기열 복원 분석
design/               화면 시안과 모집용 카드
docs/                 제품·모델·연구·운영 문서
```

## 경로 이동 시 깨지는 지점

- 컴파일된 CLI 다섯 곳이 `import.meta.url`에서 `../..`를 올라가 저장소 루트를 찾는다. 디렉터리가 한 단계 깊어지면 `dist/`를 루트로 잘못 잡는다.
- 브라우저 코드는 번들링되지 않는다. `app.js`의 상대 import가 가리키는 `dist/packages/domain`도 정적 사이트에 같은 구조로 복사해야 한다.
- Vercel은 루트 `api/`를 함수 진입점으로 쓴다. 실제 구현만 `apps/api`로 옮기고 루트 어댑터는 남겨야 `/api/live`와 `/api/feedback`이 유지된다.
- `publish-pages.yml`과 `scripts/build-site.mjs`가 사이트 조립을 각각 구현한다. 한쪽만 고치면 Pages와 Vercel의 화면이 달라진다.
- 수집 워크플로는 컴파일된 `schedule.js`와 `collector.js` 경로를 문자열로 직접 참조한다.

## 문서 구조 문제

- `docs/README.md`는 번호순 독서를 안내하지만 모델 입문자는 마지막 문서인 07부터 읽으라고 한다.
- 현행 명세, 미래 설계, 날짜가 고정된 검증 보고서, 시간순 연구 일지가 같은 디렉터리에 있다.
- `03-boarding-model-v2.md`, `04-queue-recovery.md`, `07-model-primer.md`가 검열·구간합 역산·대기열 복원을 반복해서 설명한다.
- `01-proposal.md`는 기획서·인터뷰 원문·향후 일정·승인 판정을 한 문서에 담아 7.1과 8장이 결번으로 남았다.
- `DEPLOY.md`는 현재 절차와 7월의 외부 상태, 장애 회고를 함께 적어 시간이 지나면 사실과 충돌한다.

## 바로잡아야 할 내용

- 루트 README는 대기 인원 예측을 서비스에 쓰지 않는다고 하지만 화면은 고정 λ로 대기 범위를 표시한다.
- 좌석 예보 MAE가 문서마다 4.34와 4.19로 다르다. 실행 날짜와 표본 범위를 구분해야 한다.
- 현행 설계 문서의 상태공간 모형·계층 효과·공동 시뮬레이션·기대손실 추천은 아직 구현되지 않았다.
- 대기열 연구 문서는 화면의 ±35% 범위와 λ 값이 불안정하다고 지적하지만 코드에는 그대로 남아 있다. 문서에서 이를 잠정 규칙으로 명시해야 한다.
- 배포 문서는 `/api/feedback`과 Neon을 현재 구조 설명에 포함해야 한다.

## 변경 전 검증 기준선

- 타입 검사: 통과
- 테스트: 47개 통과
- 호출 예산: 수요일 최대 924회, 여유 76회
- 재방문 집계 자체검사: 통과
- 웹 데이터 파일: `latest.js`, `daily.js`, `history.js`, `profile.js` 네 개

## 참고할 기존 기록

이전 서비스화 계획과 조사 기록은 `docs/archive/worklogs/`에 원문 그대로 보관한다. 새 문서에서는 임시 작업 기록을 근거 문서로 사용하지 않는다.

## 작업 중 발견

- 로컬 환경에는 `curl`이 없으며 zsh의 `status`는 읽기 전용 변수다. HTTP 산출물 검증은 Node 내장 `fetch`를 사용한다.
- `backtest`, `queue`, `score`에는 도움말 전용 분기가 없다. `--help`도 일반 실행으로 처리한다. 현재 작업공간에는 비공개 원천 데이터가 없어 데이터 기반 CLI 전체 실행은 할 수 없다.
- 셸 검색식에 백틱이 필요하면 큰따옴표를 쓰지 않는다. zsh가 명령 치환으로 해석하므로 작은따옴표 패턴을 사용한다.
- 설치된 `humanize-korean`의 `prepare_monolith_input.py`는 상대 `--run-dir`를 호출한 저장소가 아니라 플러그인 설치 루트 기준으로 해석한다. 이 저장소의 `_workspace`를 처리할 때는 절대경로가 필요하다.
- 작업 기록 패치는 먼저 대상 파일의 실제 절 제목과 마지막 문맥을 확인해야 한다. 예상한 제목으로 여러 파일을 한 번에 패치하면 한 곳의 문맥 불일치로 전체 패치가 취소된다.
- 문서 일관성 감사가 자체 하위 감사를 병렬 실행하면 루트에서 새 윤문 에이전트를 시작할 동시 작업 자리가 남지 않는다. 감사 결과를 먼저 받은 뒤 완료된 에이전트를 재사용하면 된다.
- 서로 겹치는 `sed` 범위를 이어 출력하면 경계 줄이 두 번 보일 수 있다. 중복을 고치기 전에는 단일 범위나 `rg`로 실제 파일의 발생 횟수를 확인해야 한다.
- `apps/web/src/app.ts`는 1,434줄 안에 UI·폴링·지도·설문뿐 아니라 상류 추천 임계값, 정류장당 이동 시간, 대기열 해소 추론을 함께 둔다. 현재 디렉터리 구조는 정리됐지만 도메인 규칙 집중 원칙을 만족하려면 이 순수 규칙을 `packages/domain`으로 옮겨야 한다.
- 함수를 도메인으로 옮길 때 실행 호출뿐 아니라 `ReturnType<typeof ...>`처럼 타입 위치에서 참조하는 옛 이름도 함께 검색해야 한다.
- 예측·대기열 회귀 감사에서 현재 정류장 수요 선반영, 도착 평균 시점, 하류 전파, 정류장당 2분, 회차 경계, 해소 추론과 60분 경계는 유지됐다. 다만 발행 로그는 미정차 지점을 포함한 좌석 값만 기록하고 화면은 이를 숨긴 뒤 만석 연속 수로 최종 판정을 만든다. 발행 시각과 화면 열람 시각도 다르므로 라이브 채점을 화면 전체 판정의 검증으로 해석하면 안 된다. 구조 개편 중 제품 규칙을 바꾸지 않는 D6에 따라 출력은 유지하고 문서에 범위를 명시했다.
