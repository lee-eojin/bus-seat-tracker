# 저장소 구조와 문서 체계 개편 계획

## 목표

실행 책임이 디렉터리 경계에 드러나도록 저장소를 재구성하고, 문서를 독자와 용도별로 나눈 뒤 자연스럽고 충분한 설명으로 다시 쓴다.

## 현재 Phase

**완료**

## Phase 1 — 현황 감사

상태: 완료

- [x] TypeScript·JavaScript import와 실행 진입점 조사
- [x] Vercel·GitHub Actions·정적 사이트 조립 경로 조사
- [x] 문서 역할·현행성·중복·충돌 조사
- [x] 변경 전 타입 검사와 테스트 기준선 확보

## Phase 2 — 소스와 실행 경로 재배치

상태: 완료

- [x] 웹을 `apps/web`으로 이동
- [x] API 구현을 `apps/api`로 이동하고 루트 `api/`에는 Vercel 어댑터만 유지
- [x] 공용 규칙을 `packages/domain`으로 이동
- [x] 수집기를 `tools/collector`, 검증 도구를 `research/backtest`로 이동
- [x] 실행 명령·TypeScript 설정·ignore 규칙 갱신
- [x] Pages와 Vercel의 사이트 조립 경로를 하나로 통일
- [x] 기존 `/prototype-bus/` 주소의 호환 경로 유지

## Phase 3 — 문서 정보 구조 개편

상태: 완료

- [x] `product`, `model`, `design`, `research`, `operations`, `archive`로 분류
- [x] 현재 구현과 미래 설계를 분리
- [x] 날짜가 고정된 검증 결과를 연구 기록으로 분리
- [x] 루트 README와 문서 안내서를 새 구조에 맞게 작성
- [x] 코드·워크플로·디자인 산출물의 문서 참조 갱신

## Phase 2 보완 — 브라우저 도메인 휴리스틱 분리

상태: 완료

- [x] `app.ts`의 추천 임계값과 후보 선택을 도메인 모듈로 이동
- [x] 정류장당 이동 시간과 대기열 해소 추론을 도메인 모듈로 이동
- [x] 추출한 순수 규칙에 경계값 테스트 추가
- [x] 화면에는 브라우저 상태 조립과 렌더링만 남기고 동작 회귀 확인

## Phase 4 — 내용 보강과 문체 정리

상태: 완료

- [x] 구현된 동작과 아직 구현하지 않은 설계를 명시
- [x] 서로 다른 검증 지표의 날짜·표본 범위를 구분
- [x] 배포 절차에서 외부 상태와 사고 회고를 분리
- [x] 주요 문서를 `humanize-korean`으로 윤문
- [x] 수치·고유명사·직접 인용·의미 보존 검증

## Phase 5 — 전체 검증

상태: 완료

- [x] 구경로와 깨진 문서 링크 전수 검색
- [x] `npm run typecheck`
- [x] `npm test`
- [x] `npm run budget`
- [x] `npm run retention -- --self-test`
- [x] 데이터 번들과 사이트 산출물 검증
- [x] 로컬 HTTP 환경에서 정적 페이지와 모듈 경로 확인

## 후속 — 수학 입문서 복원

상태: 완료

- [x] 보관 위치의 수학 입문서를 `docs/model`로 이동
- [x] 현행 문서 안내와 모델 개념에서 눈에 띄게 연결
- [x] 옛 문서 번호와 코드 경로를 현재 구조에 맞게 갱신
- [x] 문서 링크와 구경로 잔존 여부 확인

## 결정 사항

| 번호 | 결정 | 선택 | 근거 |
|---:|---|---|---|
| D1 | 개편 범위 | 저장소 구조와 문서를 함께 전면 개편 | 사용자 선택 3번 |
| D2 | 배포 API 위치 | 구현은 `apps/api`, 진입점은 루트 `api/` | Vercel 파일시스템 라우팅 계약을 유지하면서 책임 분리 |
| D3 | 웹 공개 경로 | 루트를 기본 주소로 사용하고 `/prototype-bus/` 호환 경로 제공 | 낡은 이름을 제거하되 기존 링크는 깨뜨리지 않음 |
| D4 | 문서 분류 기준 | 현재 동작·설계·연구·운영·보관을 분리 | 문서의 시간축과 독자가 섞여 생긴 중복·모순 해소 |
| D5 | 사이트 조립 | Vercel과 Pages가 같은 조립 스크립트 사용 | 두 구현의 경로 불일치 방지 |
| D6 | 코드 동작 | 구조 변경 중 도메인 계산과 제품 규칙은 바꾸지 않음 | 경로 회귀와 모델 변경을 분리해 검증 가능하게 유지 |
| D7 | 수학 입문서 | `docs/model/mathematical-primer.md`로 현행 복원 | 사용자 선택 1번, 수식을 한곳에서 찾는 장점 유지 |

## 에러 로그

- 2026-08-05: 로컬 HTTP 응답 확인에 `curl`을 사용했으나 실행 환경에 명령이 없었고, zsh 예약 변수 `status`도 사용했다. 같은 명령을 반복하지 않고 Node 내장 `fetch`로 전환했다.
- 2026-08-05: `backtest`, `queue`, `score`는 `--help`를 지원하지 않아 실제 입력을 읽으려 했고, 로컬 비공개 데이터가 없어 종료됐다. 새 기본 경로 `/data`까지는 정확히 계산됐으며, 원천 데이터가 필요한 실행 검증은 반복하지 않는다.
- 2026-08-05: 구경로 검색 정규식의 백틱을 큰따옴표 안에 넣어 zsh가 명령 치환으로 해석했다. 파일 변경은 없었고, 같은 검색은 작은따옴표 패턴으로 바꾼다.
- 2026-08-05: `humanize-korean` 준비 스크립트에 저장소 기준 상대 `--run-dir`를 넘겼지만, 설치된 스크립트는 플러그인 루트를 기준으로 해석해 입력을 찾지 못했다. 저장해 둔 입력은 유지하고 절대경로를 넘기는 방식으로 바꾼다.
- 2026-08-05: 위 오류를 기록하는 첫 패치가 `findings.md`의 실제 절 제목과 맞지 않아 적용되지 않았다. 파일을 다시 읽고 문서 끝의 확정된 문맥을 기준으로 한 번에 기록했다.
- 2026-08-05: 문제 검증 문서의 light 윤문을 별도 에이전트에 맡기려 했지만, 일관성 감사 에이전트가 하위 감사를 병렬 실행 중이라 동시 작업 한도에 걸렸다. 실행 중인 작업을 중단하지 않고 자리가 생긴 뒤 기존 에이전트를 재사용한다.
- 2026-08-05: 여러 사실 오류를 한 패치로 고치면서 겹쳐 읽은 출력의 문장을 실제 중복으로 오인해 삭제 문맥을 넣었고, 해당 문맥이 없어 전체 패치가 취소됐다. 파일별 작은 패치로 나눠 한 곳의 문맥 오류가 다른 수정을 막지 않게 바꾼다.
- 2026-08-05: 대기열 계산을 `estimateQueueAt`으로 옮긴 뒤 `queueLabel`의 타입 선언 한 곳이 옛 `estimateQueue` 이름을 참조해 타입 검사가 실패했다. 남은 참조를 새 함수 이름으로 바꾸고 다시 검사한다.
- 2026-08-06: 문서 보정 에이전트가 파일 수정을 마친 뒤 사용량 제한으로 최종 보고에 실패했다. 반영된 파일을 직접 대조하고 남은 표현 세 곳을 로컬에서 마무리한다.
