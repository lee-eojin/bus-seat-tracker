# Task Plan: v2 층-1 백테스트 + 지속 캡처 (확정 2026-07-23)

## 확정 범위 (사용자 동의)
1+2 진행. 단 2번은 "학습 파이프라인"이 아니라 **검증·편향 측정 계측기**로 규정.
- 근거: 산발적·기준정류장 수집은 §5.2 체계 수집 미충족 → §17대로 층-1 정착이 정직.
- 산출: ① 검증된 좌석 도착 예보(정직한 출력=좌석 여유 하한) ② 좌석 신호 편향 실측.
- 비산출: "몇 명 대기" 예측기 (체계 수집 감수 시에만 열림).

## 실행 순서
### Phase 1 — 층-1 백테스트 (완료 2026-07-23)
- [x] private 데이터 scratchpad 클론 (19~23일 5일)
- [x] shared/profile.ts 추출 + build-data.ts 리팩터 → 출력 바이트 동일 검증 완료
- [x] backtest/backtest.ts (rolling-origin 3폴드, 관측쌍 4,836건)
- [x] npm run backtest 스크립트, typecheck 통과

## Phase 1 결과 (평일 3폴드, 4,836쌍, 만석 8.9%)
- 도착 좌석 MAE: profile-propagate 4.34 < naive-persist 6.91 (모든 horizon에서 우세).
  → 순수요 프로파일이 "좌석 그대로" 기준선을 이김 (§11.4 기준선1 통과, 방향성).
- 만석 Brier: profile 0.046 < full-frequency(v1) 0.070 < naive 0.083 < conservative 0.089.
  → 만석 예측도 profile 최고, v1 만석빈도(기준선2)보다 나음.
- 예측구간 포함률 72.2% (목표 80%) → 구간이 약간 좁다(과소 포함).
- 만석확률 보정: 전 구간 과소예측([0.2,0.5)→실제0.52, [0.5,1)→실제0.89).
  → 모델이 혼잡을 과소평가(under-confident). v2 §1.4(만석표본 배제·검열이 수요를
    낮게 추정) 예측과 일치. 부록 A.4 미입증 항목의 첫 실측 신호.
- 한계: 3폴드 소량, 7/23 오전만(부분), 운행 재구성 정확 가정(Phase 0 전).

## 커밋 분리 (완료 2026-07-23)
- 변경 A 통근 스케줄: PR #4 스쿼시 머지 완료 → main f935c8c. 브랜치 삭제됨.
  다음 collect 크론부터 새 창 적용.
- 변경 B 백테스트 (eb1fe3e): feature/layer1-backtest (bd10ddd 기반, 로컬 전용, 미push).
  main이 f935c8c로 전진해 B는 뒤처짐 — A와 파일 겹침 없어 PR 시 클린 머지 가능.
- 미결: B 브랜치 push/PR 여부(사용자), Phase 2 진행 여부(사용자).

### Phase 2 — 지속 캡처 (다음 청크)
- [ ] localStorage 아침 기록 + phase0 로그를 지속·내보내기 가능하게 (private repo 경로)
- [ ] 세 용도: Phase 0 API=현실 검증 / 편향 크기 측정 / 층-1 하한 보정
- [ ] 후보 정류장 대규모 수집 장치는 만들지 않음 (산발 수집 전제)

### Phase 1b — 균등 배분 제거: 구간합 역산 (2026-07-24)
- 동기: 범계역 실측 — 균등 배분(1/n)이 정류장 스파이크(실측 30 vs 기대 3.1)를 뭉갬 (§4.3).
- 모델: 구간 관측을 합 제약 Δᵢ = Σ_{k∈Kᵢ} N_k 로 두고, 정류장별 μ_k·σ²_k를
  ridge 최소제곱 좌표하강으로 역산. 무정보(완전 겹침)면 균등 해로 퇴화.
- [x] shared/profile.ts에 buildDeconvolvedProfileRoute 추가 (전파는 기존 그대로)
- [x] 합성 검증(§11.1) 통과: 스파이크 25 → 역산 23.7 복원, 균등 9.4로 뭉갬
- [x] backtest 4폴드(6,480쌍): 구간합 MAE 동급(4.14 vs 4.19), 6+구간만 역산 우세(-0.18),
      포함률 72.1→74.9%. 구간 합 평가라 뭉갬이 상쇄되는 구조적 이유 확인.
- [x] 범계역 재판정 → **중대 발견**: 역산이 스파이크를 seq56(문화의거리)에 배치
      (μ56=11.3, μ55=-0.4). 필드 기록은 55 확정 → **+1 정류장 계통 오프셋** =
      API 잔여석이 "도착(승차 반영 전)" 상태라는 실증 신호 (v2 §4.2 Phase 0 질문의 답).
      구간 귀속을 (a,b] → [a,b-1]로 바꾸면 v2 §4.7("m-1까지 전파")과 정합.
- [x] 귀속 실험 완료 (2026-07-24):
      · 합성 매트릭스 4/4 — 규약 불일치 = 정확히 ±1 정류장 이동 (진단 기계적 확증)
      · 구간합 백테스트는 규약에 **정확히 불변** (좌표 재라벨링이라 식별 불가가 수학적 필연)
        → 규약을 가르는 유일한 증거 = 필드 기록 (7/24 범계역 실측의 결정적 역할)
      · arrival+deconv: 범계역 μ=11.3으로 스파이크 위치 복원. "문화의거리 도착" 예측
        51.8석(기존 UI 방식) → 40.5석 (실측 24석 방향으로 대폭 접근)
      · 남은 갭(40.5 vs 24)은 배분이 아니라 u_day·headway 층 몫 (v2 §4.5~4.6, §10.4)
- [x] 검증 보고서 작성: docs/validation-2026-07-24.md (요약·실측·수식·백테스트·
      오프셋 실증·비식별성·한계·다음 단계. 차량 식별자 미기재 — 데이터 경계 준수)
- [ ] 후속 결정 대기: 커밋 / UI 반영(arrival 규약 + [x,m−1] 전파) / v2 부록 실증 기록

## 제약·주의
- git commit/push/merge 직접 금지 — 커밋 제목만 추천.
- build-data.ts 리팩터는 라이브 발행 의존 → 출력 바이트 동일성 확인 필수.
- 원본 차량ID(HMAC)는 scratchpad에만, 공개 repo 미유입.
- 표본: 평일 20·21·22·23(진행) = rolling-origin 3폴드. 주말 19 1일.
