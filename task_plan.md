# Task Plan: 서비스화 — 검증된 좌석 예보를 배포 가능한 서비스로

**목표**: 3330·1650 통근자가 링크 하나로 들어와 "내 정류장에 버스가 올 때 몇 석일지"를 보고 어디서 탈지 정할 수 있는 서비스를 배포한다.

**마감**: 2026-07-28 (내일) — 동작하는 배포 MVP
**오늘(7/27)**: 프로토타입 완성 (로컬에서 서버 프록시 경유로 전 기능 동작)
**스택 확정**: Node/TypeScript (`shared/` 5,068줄 재사용)

---

## 현재 Phase: **S4 — 배포 (Production 파이프라인 복구 대기)**

S1~S3은 끝났고 main에 병합됐다(PR #9, #10). Git 연동으로 푸시가 곧 배포다.

이번 요청으로 **S4a — GA4 기본 계측**을 진행한다. 측정 ID는
`G-QFFRQ5Z4XX`로 확정됐고, 정적 페이지 전체에 Google 태그를 넣은 뒤
개인정보 안내를 실제 전송 항목과 맞추고 빌드 산출물을 검증한다.

**Deployment Protection은 07-28에 해제했다.** 로그인 없이 열린다.

막힌 것은 Production 환경변수 2개다. 없어서 프로덕션 빌드가 계속 실패했고,
그 결과 `outputs-eta-fawn.vercel.app`이 `/prototype-bus/` 경로가 생기기도 전의
배포에 묶여 있다. Preview에는 변수가 있어 빌드가 되므로 지금 쓸 수 있는 건
배포별 Preview URL뿐인데, 그 주소는 배포마다 바뀐다.

### 07-28 코드 정리 (PR #16, #17, #21, #22, #23)

리뷰에서 나온 구조 문제를 정리했다. 자세한 내용은 `progress.md` 07-28 항목.

- 예보 모델이 두 벌이었다. 백테스트는 `shared/profile.ts`, 화면은 `app.ts` 복사본을
  썼다. 검증 수치가 화면을 보증하지 못하는 상태였다. 통일했고 8,736쌍 대조로
  동작 동일을 확인했다.
- 수집 스케줄이 워크플로 인라인 JS에만 있어 검산이 불가능했고 예산 표가 실제와
  어긋났다. `bus-seat-collector/schedule.ts`로 빼고 `npm run budget`이 계산하게 했다.
- 테스트가 0이었다. 스케줄 경계값 16개를 붙였다 (`npm test`).
- 수집 사이클이 연속 실패해도 창 끝까지 돌며 호출만 태우던 것을 끊게 했다.

---

## 확정 아키텍처

```
Vercel 단일 프로젝트 (도메인 하나)
│
├─ 정적  web/            ← prototype-bus 승격. 예보 계산은 여기 그대로 둔다
├─ 함수  api/live.ts     ← GBIS 프록시. 키는 서버 env
│         └ Cache-Control: public, s-maxage=30, stale-while-revalidate=60
│           CDN이 origin 호출을 노선당 30초 1회로 누른다 (동시접속자 수 무관)
└─ 빌드  private data repo clone → build:data → 데이터 번들에 포함

GitHub Actions
├─ collect-bus-seats.yml  ← 그대로 유지 (수집이 끊기면 모델이 죽는다)
└─ publish-pages.yml      ← Vercel Deploy Hook 호출로 교체 (30분마다, 48회/일)
```

**디렉터리**
```
api/live.ts        얇은 핸들러 (Vercel 규약상 루트 api/)
server/gbis.ts     GBIS 클라이언트 + plateNo 제거
web/               index.html, app.ts, manifest.json
shared/            그대로 — 서버·클라이언트 공용
```

**왜 `/api/board`(서버 예보)를 안 만드는가**: 프로파일·히스토리가 이미 정적 번들로 클라이언트에 실려 있고 `app.ts`의 예보 전파가 동작 중이다. 프록시 하나면 G1·G2가 둘 다 풀린다. 서버 예보는 DB 전환(S5-2) 때가 제 순서다.

---

## 전제: 이미 있는 것 / 없는 것

| | 상태 |
|---|---|
| 좌석 예보 모델 | ✅ 검증 완료 (MAE 4.34 vs 기준선 6.91, 만석 Brier 0.046) |
| 모바일 UI | ✅ 1,050줄, 노선 축·추천·네이버지도 길찾기 |
| 수집 파이프라인 | ✅ GitHub Actions, 피크 60초 조밀 수집 |
| 서버 | ✅ `api/live.ts` 프록시 (Vercel 설정 대기) |
| API 키 보호 | ✅ 키는 서버 env에만. 클라이언트에 `data.go.kr` 흔적 0건 |
| 실시간성 | ✅ 120초 (`s-maxage`). 30초는 호출 한도 초과라 불가 |
| 법적 표기 | ✅ `prototype-bus/legal.html` (출처·면책·개인정보) |

상세는 `findings.md`.

---

## Phase S1 — 서버 프록시 (오늘, 최우선) — 약 3시간

**이유**: G1(키 노출)·G2(실시간성)를 한 번에 푼다. 이게 없으면 공개 배포 자체가 불가능하다.

- [x] `server/gbis.ts` — GBIS 클라이언트. 키는 `process.env`에서만 읽는다
  - [x] 응답에서 `plateNo` 제거 — 화이트리스트 방식(필요한 필드만 통과)이라 새 필드가 생겨도 새지 않는다
  - [x] 노선명 → routeId는 `allowedRoutes` 상수로 고정. `busrouteservice` 호출 0회 — 프록시가 노선 API 예산을 건드리지 않는다
- [x] `api/live.ts` — `GET /api/live?route=3330`
  - [x] `Cache-Control: public, s-maxage=120, stale-while-revalidate=240` (D5 개정)
  - [x] 허용 노선 화이트리스트 (임의 routeId 프록시 방지 = 남의 쿼터로 우리 키 소모)
  - [x] GBIS 오류를 그대로 흘리지 않고 상태 코드로 정규화. 실패는 `no-store`로 캐시하지 않는다
- [x] 클라이언트 전환 — `liveApiUrl`을 `/api/live`로 교체
  - [x] `data/config.js` 키 주입 경로 제거, `__CONFIG__` 선언·README 안내 갱신
  - [ ] GBIS 장애 시 폴백 표시 — 조회 실패 시 스냅샷으로 되돌아가는 동작은 이미 있으나 화면에 알리지 않는다 (S3 에러 상태로 이월)
- [x] `vercel.json` 작성
- [ ] `vercel dev` 로컬 확인 — Vercel CLI 로그인 필요, 미실행
- [ ] 정적 빌드 배선 (`outputDirectory: site` 조립 스크립트) — 비공개 데이터 레포 클론이 필요해 **S2로 이월**
- [x] `tsc --noEmit` 통과 (`tsconfig.json` include에 `api/`·`server/` 추가)
- [x] 핸들러 검증 17건 통과 — `fetch` 스텁으로 GBIS 미호출 (쿼터 보호)
- [ ] 실제 GBIS 키로 동작 확인 — 키 필요, 미실행

**완료 기준**: 로컬에서 키 없이 브라우저를 열어도 실시간 좌석이 보이고, 네트워크 탭에 GBIS 도메인이 나타나지 않는다.

**리스크 R2 — 해결 (2026-07-27)**: 수집기가 매 사이클 노선 정보를 재조회해 `busrouteservice`를
하루 약 948회(한도의 95%) 태우고 있었다. `resolveRoutes()` 캐시로 **948 → 4회/일**.
서비스화와 무관하게 이미 위험했던 항목 — 노선을 하나만 추가해도 수집이 멈출 수 있었다.
상세는 `findings.md` §0.

**리스크 R1 (최우선 확인)**: 공공데이터포털 일 호출 한도.
현재 수집만으로 **약 480회/일** 사용 중(조밀 60초 구간 420 + 피크 36 + 시간별 24).
개발계정 기본 한도가 1,000회/일이면 프록시가 쓸 여유는 **520회/일**뿐이다.
→ 30초 캐시를 **on-demand로만** 돌리면(방문자 없을 때 0회) 운행시간 내내 켜져 있어도 노선당 최대 1,920회 — **초과한다**.
→ 대응 순서: ① 포털에서 현재 한도 확인 ② 부족하면 운영계정 신청(승인 지연 있음) ③ 그때까지 캐시 TTL을 60~90초로 올리고 피크 외 시간대는 스냅샷 표시로 폴백.

---

## Phase S2 — 데이터 공급 경로 전환 (오늘)

**이유**: 프로파일·히스토리(예보의 재료)가 지금은 정적 `.js` 파일로 번들에 박혀 있다. 서버가 읽을 수 있어야 한다.

- [x] 정적 번들 조립을 스크립트로 승격 — `scripts/build-site.mjs` (`npm run build:site`)
  - [x] 데이터 출처 두 갈래: `BUS_DATA_DIR`(로컬 확인) 또는 `BUS_DATA_REPO_TOKEN`(Vercel 클론)
  - [x] 토큰을 URL이 아닌 `http.extraheader`로 넘겨 프로세스 목록·로그에 남지 않게
  - [x] `--predictions`는 넣지 않는다 — 비공개 저장소에 커밋을 남기는 작업이라 빌드의 부수 효과가 되면 안 된다 (GitHub Actions가 계속 담당)
  - [x] 로컬 실행 확인: `site/` 11개 파일 640K, `config.js` 유입 없음
- [x] 기존 `collect-bus-seats.yml`은 **그대로 유지** — 수집이 끊기면 모델이 죽는다
- [x] `publish-pages.yml`에 Vercel 배포 훅 스텝 추가 — **교체가 아니라 병행**. 훅 시크릿이 없으면 건너뛰므로 설정 전에도 예측 로그 적재가 멈추지 않는다
- [ ] Pages 발행 스텝 제거 — Vercel 배포가 확인된 뒤에 (지금 지우면 되돌릴 곳이 없다)
- [x] 데이터 신선도 표시 — `expectedIntervalMinutes` 기반 배너가 이미 동작 중

---

## Phase S3a — 핵심 화면 디자인 시안 (진행 중)

**이유**: 오늘 판정 시스템(여유/빠듯/어려움)이 들어가면서 정류장 줄의 정보가 "예상 N석" 하나에서
넷(판정·예상석·`?`·`*`)으로 늘었다. 배치를 다시 정해야 하는데 이는 CSS 수정이 아니라 설계 문제다.
기존 화면은 `index.html` 9행에 CSS 8,206자가 한 줄로 뭉쳐 있어 목표 없이 손대기 어렵다.

**이식 가능성이 조건이다.** 시안이 기존 코드와 안 맞으면 장식이 된다.

- [x] 현재 화면 정보 구조 정리 — 무엇이 어디에 왜 있는지
- [x] 시안 제작 `design/board-mockup.html`
  - [x] 기존 CSS 변수 `--ok/--warn/--bad/--unknown` 그대로 사용
  - [x] 기존 클래스명 `stop-row`, `stop-dot`, `stop-name`, `tint-*` 유지
  - [x] 판정 4상태(여유/빠듯/어려움/미관측) + 저신뢰 표기 배치
  - [x] 실제 데이터(2026-07-27 08:31)로 채워 판정이 실제로 갈리는 화면 확인
- [x] 현재 화면과 나란히 비교 → 사용자 판단
- [x] 이식: `<style>` 추가(+1,062자) · 범례 마크업 교체 · `app.ts` 칩 생성부 교체
- [ ] 실기 렌더 확인 — 브라우저 확장 미연결로 보류. 배포 프리뷰(S4)에서 확인

**완료 기준**: 시안을 그대로 `prototype-bus/index.html`에 옮겨도 `app.ts` 수정이 필요 없다.

**범위 밖**: 첫 진입 화면, 에러·빈 상태, 길찾기 화면 — S3 나머지 항목으로 남긴다.

---

## Phase S3 — 이용자 대면 완성도 (오늘~내일)

**이유**: "실제 이용자가 모일 정도"가 요구사항이다. 지금 UI는 연구용 프로토타입 문법이 남아 있다.

- [ ] **첫 진입 개선**: 지금은 목적지 입력이 필수 관문이다 → 노선 화면을 먼저 보여주고 목적지는 선택으로
- [ ] **공유 링크**: `#route=3330&dir=up` 해시가 이미 있음 → OG 태그 + 정류장 딥링크 추가
- [ ] **PWA**: manifest + 홈 화면 추가. 통근 앱은 재방문이 전부다
- [x] **법적 표기 (필수, 누락 시 배포 불가)** — `prototype-bus/legal.html`
  - [x] 공공데이터포털 출처 표기 — `legal.html` + 푸터
  - [x] 면책 — `legal.html`로 승격. 실제와 갈리는 경우 4가지를 구체적으로 적음
  - [x] 개인정보 — 서버 수집 없음. localStorage 4종·위치 정보 처리·호스팅 접속 기록까지 명시
- [ ] **에러 상태**: 데이터 없음 / API 장애 / 운행 종료 3가지 화면
- [ ] 연구용 UI 정리: Phase 0 내보내기 버튼 등 개발자 전용 요소를 숨김 처리

---

## Phase S4 — 배포 + 검증 (내일)

**여기부터는 코드가 아니라 설정이다.**

- [x] Vercel 프로젝트 연결 — 계정에 GitHub 로그인 연결이 선행돼야 `vercel git connect`가 통과한다
- [x] Preview 환경변수 등록 — `GYEONGGI_BUS_API_KEY`, `BUS_DATA_REPO_TOKEN`
- [x] 프리뷰 배포 — 푸시 16초 만에 자동 배포, 데이터 클론·집계·번들 조립까지 통과
- [~] GitHub 시크릿 `VERCEL_DEPLOY_HOOK_URL` — **불필요해졌다.** Git 연동으로 푸시가 배포를 일으킨다
- [x] **Deployment Protection 해제** (07-28) — Vercel Authentication을 껐다. 로그인 없이 열린다
- [ ] **Production 환경변수 2개** — 없어서 프로덕션 빌드가 실패한다 (`BUS_DATA_DIR 또는 BUS_DATA_REPO_TOKEN 중 하나가 필요합니다`).
      대시보드에서 Preview 값이 읽히는지부터 확인한다. `[SENSITIVE]`면 재발급이고, 그때는 Actions 시크릿도 같이 갱신해야 수집이 안 끊긴다
- [ ] **프로덕션 도메인 복구 확인** — 환경변수를 넣고 `315a69d` 이후 커밋으로 재배포한 뒤,
      `outputs-eta-fawn.vercel.app/prototype-bus/`가 404가 아니고 `/api/live?route=3330` 응답에 `id`가 없는지 본다
- [ ] 실기기(모바일) 확인
- [ ] **아침 통근 시간 실측 1회** — 배포본으로 범계역 케이스 재현, 예보와 실제 대조
- [ ] 캐시 적중률·GBIS 호출 수 확인 (`npm run budget` 이론값 대비)
- [ ] 응답자가 말한 정류장 서열(판교역 > 백현마을1단지 > 이매촌한신)이 수집 데이터에서 재현되는지 대조

---

## Phase R — 팀원 모집 (2026-07-29 시작)

**목표**: 프론트엔드와 백엔드를 각 1명씩 찾는다.

**확정 사항**
- 조건은 무보수 사이드 프로젝트, 지분 없음. 숨기지 않고 카드와 글에 명시한다
- 모집 글은 노션에 직접 작성한다. 사이트에 페이지를 만들지 않는다
- 설득 논리는 비전이 아니라 증거다. 배포된 서비스, 실사용자 51명, 대면 설문 15명,
  검증된 모델(MAE 4.34)이 다른 모집글과 갈리는 지점이다

- [x] 소개 카드 13종 생성 (`design/cards/`). 마케팅 10종과 담백한 3종
- [ ] 노션 모집 글 작성 (사용자 직접)
- [ ] 지원 폼 준비
- [ ] 배포 채널 정하기

### 카드 생성기

`node design/cards/build.mjs` 하나로 13장을 다시 뽑는다. 화면 목업은
`design/cards/screen.mjs`가 실제 `prototype-bus` 클래스를 그대로 옮겨 쓴다.
목업이 실물과 다르면 합류한 사람이 첫날 실망하므로 값도 실제 관측에 맞췄다.
산출물은 `design/cards/out/`이고 gitignore 대상이다.

협업 보드(09)에 없는 팀원 이름을 박지 않았다. 지금은 혼자이고 그게 모집하는 이유다.
커서를 모집 중으로 두어 빈 자리를 드러내는 쪽이 정직하면서 메시지도 강하다.
수식 카드(10)는 `docs/queue-recovery.md`와 `docs/boarding-model-v2.md`의 실제 식만 쓴다.
장식으로 만든 식은 넣지 않는다. 읽는 사람이 개발자다.

---

## Phase S5 — 배포 이후 (범위 밖, 기록만)

내일 마감에는 넣지 않는다. 순서만 확정해 둔다.

1. 사용자 실측 기록 서버 수집 (localStorage → API) — 층-2 학습 루프의 유일한 입구
2. DB 전환 (git JSONL → 시계열 DB) — 이용자 늘면 조회 지연이 먼저 터진다
3. 노선 확장 (`ROUTE_NAMES` 하드코딩 제거, 온디맨드 수집)
4. λ 날짜 간 분산 측정 → 대기 인원 예측기 승격 (`docs/queue-recovery.md` §14-2)
5. u_day·headway 층 추가 (범계역 잔여 갭 40.5 vs 24)

---

## 결정 사항

| # | 결정 | 선택 | 근거 | 일자 |
|---|---|---|---|---|
| D1 | 프로젝트 성격 | 실제 배포 서비스 (오늘 프로토타입 → 내일 MVP) | 사용자 확정 | 07-27 |
| D2 | 서버 스택 | Node/TypeScript | `shared/` 5,068줄 무포팅 재사용. 마감이 하루 | 07-27 |
| D3 | 배포 플랫폼 | Vercel 단일 프로젝트 + CDN 캐시 | 마찰 최소. `s-maxage`가 동시접속자 수와 무관하게 GBIS 호출을 눌러줌 | 07-27 |
| D4 | repo 구조 | 기존 repo에 `server/`·`api/`·`web/` 추가 | `shared/`를 경로 하나로 import. 검증 문서와 서비스가 같은 이력에 남음 | 07-27 |
| D5 | 실시간 캐시 TTL | ~~30초~~ → **120초** | 일 한도 1,000회 중 수집이 482회. 출퇴근 6.5시간 기준 30초=1,560회·60초=780회로 둘 다 예산 초과, 120초=390회만 가용분(~518회) 안에 든다 | 07-27 개정 |
| D6 | 서버 예보 API | 만들지 않음 | 프로파일이 이미 클라이언트 번들에 있고 예보 로직도 동작 중. DB 전환(S5-2) 때가 제 순서 | 07-27 |
| D7 | 오늘 작업 순서 | S1 → S2 → S3 (직렬) | 프록시가 안 되면 나머지가 무의미. 동시 착수 시 원인 분리 어려움 | 07-27 |
| D8 | 시안 먼저 vs 기존 화면 개선 먼저 | 시안 먼저 (별도 파일) | 판정 시스템이 정보 구조를 바꿔 목표 정의가 선행돼야 함. 기존 파일에는 네이버지도 미커밋 작업이 얹혀 있어 되돌리기 비용도 다르다 | 07-27 |

---

## 제약·주의

- **git 규칙 개정 (07-27)**: 작업 브랜치 커밋·푸시는 직접 실행한다. `main`은 PR로만.
  `merge`·`rebase`·`push --force`·`reset --hard`는 여전히 금지. 상세는 `CLAUDE.md`.
- 원본 차량번호(`plateNo`)는 서버 응답·로그·저장 어디에도 넣지 않는다. 가명화는 HMAC, 시크릿은 API 키와 분리.
- 공개 repo에 API 키·수집 데이터·빌드 산출물 금지.
- `build-data.ts` 변경 시 출력 바이트 동일성 확인 (라이브 발행 의존).
- 수집 워크플로는 건드리지 않는다 — 하루 끊기면 백테스트 표본이 빈다.

---

## 에러 로그

**07-27 18:40~21:16 수집 중단 (2시간 36분).** `BUS_DATA_REPO_TOKEN`이 죽어 수집과 발행이 함께 멈췄다.
`Collect bus seats`는 401 Bad Credentials, `Publish seat board`는 exit 128.

원인은 PAT 재발급이었다. Vercel에 넣으려고 새로 만들었더니 GitHub Secrets에 있던 옛 값이 즉시 죽었다.
GitHub Secrets를 새 값으로 교체하고 두 워크플로를 수동 실행해 복구했다.
잃은 건 저녁 피크 끝자락이고 되돌릴 방법은 없다.

교훈은 `DEPLOY.md` 3절에 남겼다. **PAT를 재발급하면 그 토큰을 쓰는 모든 곳을 같이 갱신한다.**

**07-27 PR #8 병합 실패.** `9d2bbf2`(작업 브랜치)와 `649eabb`(main, PR #7 squash)의 내용이 같은데
커밋이 달라 6개 파일에서 충돌했다. squash 병합의 전형적인 함정이다.
main 기준 새 브랜치에 cherry-pick으로 옮겨 해결했다(PR #9). merge·rebase·force-push를 쓰지 않았다.

**07-27 `Verify forecast` 실패 (미해결).** 라이브 예측 251건이 25분보다 낡은 관측을 근거로 발행됐다는 경보.
채점 대상일이 일요일이었고, 주말은 매시 1회 수집이라 발행(30분 주기)이 최대 60분 낡은 값을 쓴다.
경보 자체는 사실이지만 임계값(`score-predictions.ts:18` `stalenessAlarmMs`)이 피크 기준으로만 잡혀 있다.
**이대로 두면 주말마다 빨간불이 뜨고, 그러면 진짜 사고를 놓친다.** 수집 간격에 따라 기준을 나눠야 한다.

---

## 부록: 이전 계획 (연구 단계, 완료·이월)

Phase 1 층-1 백테스트, Phase 1b 구간합 역산은 완료. 결과는 `docs/validation-2026-07-24.md`, `docs/queue-recovery.md`에 있다.

**이월된 미완 항목** (Phase S5로 흡수):
- Phase 2 지속 캡처: localStorage 기록을 서버로 → S5-1
- 대기 인원 예측기 승격: λ 날짜 간 분산 측정 선결 → S5-4
- v2 문서 부록 A에 오프셋 실증·규약 비식별성 기록 → 미이월, 별건
- PC 네이버지도 전체 지도 두 구간 최종 확인 → S3 에러 상태 점검에 포함
