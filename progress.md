# Progress Log

## 2026-07-27

- **10:50** 서비스화 계획 착수. 기존 코드베이스 조사 완료 (5,068줄, 파이프라인 3단, 검증 문서 4건).
- **10:55** `findings.md` 작성 — 현재 파이프라인 구조, 코드 자산, 검증된 모델 성과, 서비스화 갭 8건(G1\~G8), 차별점 3가지, 제약.
- **11:00** 사용자 결정 수신: 실제 배포 서비스 / 내일까지 동작하는 MVP / Node·TypeScript.
- **11:05** `task_plan.md` 재작성 — 연구 단계 계획을 부록으로 내리고 서비스화 Phase S0\~S5로 재구성.
- **11:05** 리스크 R1 식별: 공공데이터포털 일 호출 한도. 현재 수집만으로 약 480회/일 소진 중, 프록시 추가 시 초과 가능. 배포 전 한도 확인 필요.
- **11:15** 사용자 결정 수신: Vercel + CDN 캐시 / 기존 repo에 `server/` 추가 / S1 프록시 먼저.
- **11:20** 계획 구체화 중 S1 범위 축소 발견 — `/api/board`(서버 예보) 불필요. 프로파일이 이미 클라이언트 번들에 있고 `app.ts`의 예보 전파가 동작 중이라 `/api/live` 프록시만으로 G1·G2가 해결된다. D6으로 기록, 약 1시간 절약.
- **11:20** 확정 아키텍처와 D3\~D7을 `task_plan.md`에 반영. S1을 5개 작업 약 3시간으로 분해.
- **11:30** 사용자 확인: 일 한도 1,000회 확정. R1이 가설에서 확정 제약으로 승격.
- **11:35** 코드 정독 중 R2 발견 — `collector.ts:main()`이 `--once` 실행마다 `findRoute`(busrouteservice)와 `fetchRouteStops`(busrouteservice)를 **매번 다시 호출**한다. 노선 2개면 사이클당 busrouteservice 4회. `-stops.json`은 `backtest/data-source.ts:24`·`build-data.ts:84`가 읽지만 collector 자신은 되읽지 않아 호출 절감에 쓰이지 않는다.
- **11:35** API가 서비스 단위로 갈린다는 점 확인 (`collector.ts:12-16`): `busrouteservice`(노선·정류장) / `buslocationservice`(차량 위치). 한도 집계 단위가 서비스별인지 계정 전체인지에 따라 결론이 갈린다.
- **11:40** 회계 검증 워크플로 착수 (`wf_7588513d-40c`) — 코드 회계 / 크론 사이클 수 / 포털 한도 정책 / 클라이언트 폴링 4축 감사 → 반증 검증 → 완화 전략 4관점 → 종합.
- **11:50** R2 수정 착수 (사용자 지시: 노선 정보 캐싱부터).
- **12:10** `bus-seat-collector/collector.ts` 수정 완료 (+66/−5).
  - `resolveRoutes()` 추가 — 노선 번호로 캐시를 찾아 신선하면 `findRoute`·`fetchRouteStops`를 모두 건너뛴다.
  - `loadCachedRoutes()` — 캐시 파일명이 노선 ID라 디렉터리를 훑어 번호로 색인. 파싱은 `shared/model.ts:readRouteCache` 재사용(중복 구현 안 만듦).
  - `isCacheFresh()` — 기본 24시간, `ROUTE_CACHE_HOURS`로 조정. `--refresh-routes`로 강제 갱신.
  - 캐시 미스일 때만 `cacheRouteStops` 호출 → 파일이 사이클마다 다시 쓰이지 않는다.
- **12:15** 검증 6/6 통과 (`scratchpad/verify-route-cache.sh`). 신선한 캐시 / 만료 / 강제 갱신 / 디렉터리 부재 / 깨진 JSON 5개 사례.
  - 판정 근거 둘: 재사용 로그, 그리고 캐시 파일 mtime 불변(= `cacheRouteStops` 미호출).
  - 신선한 캐시에서 401이 차량 위치 API에서만 발생 → 노선 API 미호출 확인.
  - 실제 API 키 미사용 (잘못된 키는 사용자 쿼터에 잡히지 않음).
- **12:15** `npm run typecheck` 통과.
- **부수 효과**: `routes/*-stops.json`이 매 사이클 재기록되지 않으므로 데이터 저장소의 사이클별 커밋에서도 빠진다.
- **12:40** 회계 검증 워크플로 완료 (에이전트 11개 중 10개 성공, **포털 정책 조사 1개는 연결 끊김으로 실패** → 한도 집계 단위 여전히 미확정).
  - 사이클 수 확정: 하루 **241회**(추정 237에서 정정). `busrouteservice` 낭비분 **964회(96.4%)**.
  - `buslocationservice` 482회(48.2%), 웹 가용분 518회(헤드룸 100 제외 시 418회).
  - 현재 배포본은 API 0회 사용 — `publish-pages.yml`이 `config.js`를 만들지 않아 키가 없다.
  - **현재 코드로 키를 넣어 배포하면 가시 탭 하나(30초 폴링, 120회/시)만으로 5시간 만에 한도 초과** → 연구 수집까지 동반 정지.
  - 개발계정에서 30초 신선도는 불가능. 출퇴근 창 6.5시간 기준 s-maxage 30초=1,560회, 60초=780회, **120초=390회**만 예산 안.
  - `busarrivalservice`(15080346) 미신청 → +1,000회 여유 존재, 자동승인.
- **12:45** 워크플로가 지적한 캐시 결함(C1) 재현 확인 — TTL 만료 + 노선 API 실패가 겹치면 쓸 수 있는 캐시를 두고도 `Promise.all` reject로 좌석 수집까지 정지. 스냅샷 0건.
- **13:10** 캐시 복원력 보강 완료 (`collector.ts` +94/−5 누적).
  - **만료 캐시 폴백**: 노선 갱신 실패 시 캐시가 있으면 경고 후 그것으로 수집 계속. "노선 정보를 갱신 못 함"과 "좌석을 관측 못 함"을 다른 사건으로 분리.
  - **부분 수집**: 해석 실패한 노선만 제외하고 나머지는 수집. 전부 실패할 때만 중단. 노선 추가가 기존 수집을 멈추지 못하게.
  - **중복 캐시 선택**: 같은 노선 번호 파일이 둘이면 `readdir` 순서가 아니라 `cachedAt` 최신으로.
  - **`--refresh-routes` 발판 제거**: 캐시를 버리지 않고 건너뛰기만 해서, 강제 갱신이 실패해도 폴백 가능.
  - `cachedAt` 파싱 불가 시 0으로 취급해 만료 처리.
- **13:10** 검증 16/16 통과 (10개 사례). `npm run build` 종료코드 0.
- **적용하지 않은 권고**: 워크플로의 "내용이 달라졌을 때만 `writeFile`" — 적용하면 `cachedAt`이 갱신되지 않아 TTL이 영구 만료 상태가 되고 **매 사이클 재조회로 964회/일에 복귀**한다. 현재 쓰기는 하루 1회라 이미 무해.
- **대기 중**: S1 프록시 착수 신호. 사용자 확인 필요 항목 — 포털 마이페이지에서 15080648·15080662 잔여 트래픽 실측(한도 단위 확정), `busarrivalservice` 활용신청.
- **15:43** 디자인 시안 계획 착수. 현재 화면 조사 — `index.html` 25행 중 9행에 CSS 8,206자(선택자 89개)가 한 줄. 디자인 토큰(`--ok/--warn/--bad/--unknown`)은 이미 있으나 손대기 어려운 형태.
- **15:43** D8 결정: 시안 먼저(별도 파일). 근거 — 판정 시스템이 정류장 줄 정보를 1개→4개로 늘려 배치 재설계가 필요하고, 기존 파일에 미커밋 작업이 얹혀 있어 직접 수정은 되돌리기 비용이 크다.
- **15:43** Phase S3a 신설. 범위: 핵심 화면 하나(노선 축 + 정류장별 판정). 이식 가능성을 완료 기준으로 못 박음 — 기존 CSS 변수·클래스명 유지, `app.ts` 무수정.
- **15:45** `design/board-mockup.html` 작성. 현재 화면과 제안을 나란히 배치, 2026-07-27 08:31 실제 데이터로 채움.
  - 기존 토큰(`--ok/--warn/--bad/--unknown`)과 클래스명(`stop-row`, `stop-dot`, `stop-name`, `tint-*`) 그대로 사용 — 이식 시 `app.ts` 무수정이 목표.
  - 변경 6건: 판정 배지 승격 / 좌석을 보조 텍스트로 / `?`를 점선 테두리로 / 틴트 절반 이하 / `*`를 말로 풀기 / 범례를 화면 안으로.
- **15:50** 시안 이식 완료. `index.html` 스타일 +1,062자, 범례 마크업 교체(좌석 구간 → 판정 설명), `app.ts` 칩 생성부를 배지+좌석 구조로 교체.
  - `verdictLabel()` 제거 — `?` 접미사가 점선 테두리로 대체됨.
  - 검증: `tsc --noEmit` 통과 · CSS 중괄호 108/108 균형 · tint 4종 모두 덧붙인 규칙이 최종 적용됨(나중 규칙 승리) 확인.
  - 브라우저 확장 미연결로 실기 렌더 확인 실패 → 실제 CSS로 미리보기 페이지를 만들어 대체 검증.
- **17:59** S3a 시안 이식 완료. 이식 중 `index.html`에 배지 CSS가 **이미 반영돼 있던 것을 모르고 중복 추가** — 선택자 122개(중복 4쌍)까지 갔다가 104개로 정리. 값이 같아 화면은 정상이었으나 한쪽만 고치면 안 먹는 함정.
  - 실제 변경은 `app.ts` 칩 생성부(`stop-prob` 한 칸 → `verdict` 컨테이너 + 배지 + 좌석)와 푸터 문구(없어진 `?`·`*` 기호 설명 제거).
- **17:59** S1 착수. `server/gbis.ts` + `api/live.ts` 신규, 클라이언트를 프록시로 전환.
  - **D5 개정**: 캐시 TTL 30초 → 120초. 30초는 출퇴근 6.5시간에 1,560회로 가용분(\~518회)의 3배.
  - `plateNo` 제거를 화이트리스트 방식(필요 필드만 통과)으로 구현 — GBIS가 새 필드를 추가해도 새지 않는다.
  - `allowedRoutes` 상수로 노선명→routeId 고정. 프록시가 `busrouteservice`를 0회 호출한다.
  - `tsconfig.json` include에 `api/`·`server/`가 빠져 있어 첫 typecheck가 두 파일을 통째로 건너뛰었다. 추가 후 재검사.
  - 검증 17건 통과 (`scratchpad/proxy-test.mjs`). `fetch` 스텁으로 실제 GBIS 미호출 — 테스트가 쿼터를 쓰지 않게.
  - **미검증**: `vercel dev`(CLI 로그인 필요), 실제 키 동작(키 필요). 정적 빌드 배선은 데이터 레포 클론이 필요해 S2로 이월.
- **18:15** S2 착수·완료. `scripts/build-site.mjs`로 정적 번들 조립을 승격 (`npm run build:site`).
  - `publish-pages.yml`의 조립 절차를 그대로 옮김 — 산출물이 갈리면 배포본과 Pages 발행본이 달라진다.
  - 데이터 출처 두 갈래: `BUS_DATA_DIR`(로컬) / `BUS_DATA_REPO_TOKEN`(Vercel). 토큰은 URL이 아니라 `http.extraheader`로 — URL에 넣으면 프로세스 목록과 git 로그에 남는다.
  - `--predictions`는 빌드에서 뺐다. 비공개 저장소에 커밋을 남기는 작업이라 빌드 부수 효과가 되면 안 되고, GitHub Actions가 계속 담당한다.
  - 로컬 실행 확인: `site/` 11개 파일 640K, `config.js` 유입 0.
  - `publish-pages.yml`은 **교체가 아니라 병행**으로 갔다. Vercel 배포가 확인되기 전에 Pages를 지우면 되돌릴 곳이 없다.
  - 배포 훅 스텝의 `if: ${{ secrets.X != '' }}`가 동작하지 않는 것을 발견 — `secrets` 컨텍스트는 `if:`에서 못 쓴다. env로 받아 셸에서 판정하도록 수정.
  - `.gitignore`에 `site/` 추가.
- **18:20** 배포 위험 하나 사전 검증: `api/live.ts`가 `../server/gbis.js`를 import하는데 esbuild가 `.js`→`.ts`를 해석하지 못하면 Vercel 함수 빌드가 깨진다. esbuild 0.24로 실제 번들해 확인 — 해석 성공, `server/`·`shared/`까지 완전 번들, 외부 의존 0, `plateNo` 미포함.
- **18:20** `DEPLOY.md` 작성. Vercel 프로젝트 생성 → 환경변수 2개 → GitHub 시크릿 1개 → 확인 5단계 → 호출 예산 → Pages 정리 순.
  - 확인 5단계의 핵심은 ④ '브라우저 네트워크 탭에 data.go.kr 없음' — S1의 완료 기준이자 이 배포의 존재 이유.
  - Pages 스텝 제거는 Vercel 배포 확인 **후**로 명시. 먼저 지우면 되돌릴 곳이 없다.
- **18:23** 법적 표기 작성 (`prototype-bus/legal.html`). 배포 차단 요소 3건 해소.
  - 문구를 쓰기 전에 코드를 먼저 감사: localStorage 4종(목적지·승차 정류장·탑승 기록·관측 로그), `getCurrentPosition`은 네이버지도 링크 좌표로만 쓰이고 서버 미전송, 서버로 가는 값은 노선 번호뿐.
  - 호스팅 접속 기록(Vercel)은 남는다는 사실을 숨기지 않고 명시. "아무것도 수집 안 함"은 사실이 아니다.
  - 면책은 일반 문구 대신 실제로 갈리는 경우 4가지로 — 증차·결행, 다른 노선 대기자, 표본 부족 시간대, API 지연.
  - `build-site.mjs`와 `publish-pages.yml` 양쪽에 복사 대상 추가. 한쪽만 넣으면 두 배포본이 갈린다.
- **20:40** 미정차 지점 처리. 사용자가 `(경유)` 정류장은 못 탄다고 지적한 데서 시작해 버그 네 군데가 한 원인이었음을 확인.
  - 표기가 `(경유)` → `(미정차)`로 바뀌어 있었다. 한쪽만 보던 `passThroughAfter()`가 0건을 반환하며 조용히 죽어 있었고, `app.ts`에는 판정 자체가 없었다.
  - 화면 판정, 길찾기 버튼, **추천 로직**, 분석 도구 넷이 전부 같은 원인. 추천은 1650의 미정차 24곳 전부에 히스토리 표본이 있어(1,841건) 실제로 발동할 수 있는 상태였다.
  - `shared/model.ts`의 `isNonBoardingStop` 한 곳으로 모으고, 표기가 또 사라지면 수집기가 경고하도록 회귀 감시 추가.
- **21:00** 커밋 정리·푸시. 작업 브랜치 커밋·푸시 권한을 받아 처음으로 직접 실행.
  - `~/.claude/CLAUDE.md`를 "기본 금지 + 프로젝트가 명시 허용하면 예외"로 고치고 저장소에 `CLAUDE.md` 추가. `merge`·`rebase`·`force-push`는 계속 금지.
  - 커밋 순서를 뒤집었다. `app.ts`가 `shared/model.ts`의 새 심볼을 쓰므로 미정차 커밋이 먼저 가야 중간 커밋이 빌드된다.
- **21:16** **수집 장애 복구.** `BUS_DATA_REPO_TOKEN` 만료로 18:40부터 수집·발행이 멈춰 있었다. 상세는 `task_plan.md` 에러 로그.
- **21:30** Vercel Git 연동. 계정에 GitHub 로그인 연결이 없어 `vercel git connect`가 400으로 막혔고, 연결 후 통과.
  - `vercel project inspect`는 Git 연결 여부를 출력하지 않는다. 이걸 근거로 "미연결"이라 잘못 판단했다가 `vercel git connect`의 `already connected`로 정정.
  - 푸시 16초 만에 Preview 자동 배포. 빌드 로그로 데이터 클론·집계·번들 조립까지 확인.
- **21:50** PR #8 병합 실패 → cherry-pick으로 우회해 #9로 병합. 상세는 에러 로그.
- **22:10** README 재작성. "무엇을 하는 물건인지"가 없었다. 입석이 없어 좌석 0이면 지나간다는 사실을 앞에 놓고 다시 씀.
  - `shared/boarding.ts`·`backtest/`·`api/`·`server/`·`scripts/`가 구성 목록에 아예 빠져 있던 것도 채움.
- **22:30** 기획서 작성 (`docs/proposal.md`). 승인 조건 3가지를 정직하게 자기평가 — 타겟 정의는 충족, 문제 검증은 미흡(인터뷰 1회 시도·거절), 해결책 검증은 부분.
  - 시장 근거를 뉴스에서 확보. 6801번 증차 확정(8/3 시행)이 7/24 판교역에서 관측한 바로 그 노선이었다.
  - 나무위키 입석금지 항목의 "중간정류장 승객들이 기점으로 거슬러 가서 탄다"가 우리 추천 기능과 같은 행동 — 없던 습관을 만들 필요가 없다는 근거.
  - 제로섬 반박을 질문에서 답으로 바꿈. 퇴근 시간 하차 집중 구간은 아무도 안 쓰던 좌석이라 남의 자리를 뺏지 않는다.
- **22:50** **시장 규모 측정** (`docs/market-size.md`). 기획서의 "수백 명 단위"가 감이었고 자릿수가 틀렸다.
  - 온전한 평일 4일 기준 두 노선 승차 하한 **6,428명/일**. 만석 시작 지점(seq20) 하류 승차가 **5,596명(87.1%)**.
  - 피크 만석 차량이 3330은 42대 중 21\~28대, 1650은 32대 중 18\~22대. 운행의 절반 이상이 어딘가에서 만석이 된다.
  - 만석 "관측 횟수"는 싣지 않았다. 수집 주기에 좌우돼 60초로 모은 날은 몇 배로 뛴다. 차량 수·정류장 수만 사용.
  - 승차 하한은 좌석 감소분 합이라 승하차가 겹치면 상쇄된다. 실제는 이보다 많다.

## 2026-07-28

- **00:20** 스레드에 통근 사례를 물어 한 시간 만에 조회 810, 응답 5명. 그중 3명이 스스로 3330을 지목했고 전원이 앞 정류장으로 거슬러 간 경험을 먼저 말했다.
  - 기획서 §6의 핵심 질문("이 행동이 이미 있는가")이 유도 없이 확인됐다. 습관을 새로 만드는 문제가 아니라 이미 하는 행동을 돕는 문제다.
  - 한 응답자가 나무위키의 악순환을 자기 말로 설명했다. 범계역으로 옮기니 원래 범계역 승객이 전전 정류장으로 밀려나더라는 것.
  - 퇴근 방향 난이도 서열도 얻었다. 판교역 > 백현마을1단지 > 이매촌한신. 어디서 탈지 기능이 내야 할 출력의 정답표다.
  - 못 얻은 건 빈도. 댓글은 불만이 큰 쪽으로 치우친다. QR로 무작위에 가깝게 받아야 한다.
- **09:50** Deployment Protection 해제. Vercel Authentication은 "로그인한 팀 멤버"만 통과시켜 사실상 본인만 볼 수 있었다.
- **10:10** 공개 전 데이터 경계 감사. 정적 번들(`prototype-bus/data/*.js`)은 `id`가 전부 null, 차량번호 패턴 0건, HMAC 흔적 0건.
  - `server/gbis.ts`가 GBIS `vehId`를 그대로 내보내고 있었다. `plateNo`만 막혀 있었고 원본 차량 ID는 통과했다.
  - 클라이언트는 그 값을 쓰지도 않았다(`app.ts`가 받자마자 `id: null`로 버림). 필드를 아예 제거 (PR #15).
- **11:00** 프로덕션 도메인이 화석임을 확인. `outputs-eta-fawn.vercel.app`은 `/prototype-bus/`가 404이고 `/api/live`가 없다.
  - 원인은 Production 환경변수 부재. Preview에만 있어 Preview 빌드만 성공하고 Production은 계속 실패, 마지막 성공본이 그대로 남았다.
- **20:00** 전체 리뷰. 죽은 코드는 거의 없었으나(미사용 export 0, 참조 없는 문서 0) 구조 문제가 셋 나왔다.
  - **예보 모델 이중화**: 백테스트와 정적 예측은 `shared/profile.ts`, 화면은 `app.ts` 복사본. `normalCdf`는 8줄이 글자 단위로 같았다.
    MAE 4.34·Brier 0.046은 shared로 잰 값인데 사용자가 보는 건 복사본이었다. 상수가 같아 결과는 같았지만 한쪽만 손대면 조용히 갈라진다.
    통일 후 실제 프로파일로 **8,736쌍 대조, 불일치 0, 최대 오차 0**. `app.ts` 60줄 감소 (PR #17).
  - **스케줄이 YAML 안**: 창·간격·운행시간·실패정책 약 200줄이 인라인 JS라 테스트도 검산도 불가능했다.
    `bus-seat-collector/schedule.ts`로 추출, 모든 함수가 시각을 인자로 받게 함. 추출 전후 **10,080시점 1분 단위 대조, 불일치 0** (PR #21).
  - **예산 숫자가 8곳에 하드코딩**: 낮 창 추가로 수집이 늘었는데 갱신된 건 DEPLOY.md뿐이었다.
    `npm run budget`이 스케줄 정의에서 직접 계산하게 하고 나머지는 참조만 남겼다. 한도 초과 시 종료 코드 1.
- **20:30** 손계산이 틀렸던 것이 계산기로 드러났다. 평일 **249→253사이클(498→506회)**, 수요일 **263→267(526→534회)**.
  - 빠뜨린 자리 둘: 창을 인수한 실행이 창 시작 전 1회 더 수집, 창이 끝날 때 대기 중이던 정시 실행이 풀려 스냅샷 1회.
  - **수요일 여유가 84회가 아니라 76회다.** 캐시 TTL을 건드리기 전에 반드시 계산기를 돌려야 한다.
- **20:45** 테스트 0 → 16 (`npm test`). 창 불변식, `activeWindow` 인수 경계, 조밀 구간 경계, 수요일 한정 보정, 서비스 시간, KST 날짜 변환 (PR #22).
- **21:00** 수집 사이클 실패 시 중단 추가. 07-27 사고 때 51사이클이 연속 401로 실패하고도 창 끝까지 돌아 102회를 태우고 데이터를 0건 남겼다.
  `core.warning`이라 실행이 success로 끝나 드러나지도 않았다. 인증 실패는 1회, 그 밖은 연속 3회에서 끊고 `core.setFailed` (PR #16).
- **21:15** Phase 0 내보내기 버튼을 `?dev`에서만 노출. 리뷰어에게 보여주는 화면에 개발자 버튼이 보이고 있었다 (PR #23).
- **21:30** 브랜치 정리. 원격 18개와 로컬 7개가 전부 main에 반영됐음을 파일 단위로 확인하고 삭제. 남은 건 `main` 하나.
  - 판정이 애매하던 옛 브랜치 4개(`dense-collection`, `kakao-route-link`, `layer1-backtest`, `proxy-deploy`)도 기여분이 main에 있음을 확인했다.

## 2026-07-29

- **GA4 연동 착수**: 측정 ID `G-QFFRQ5Z4XX` 확정. 기존 Google 태그는 없고,
  배포 빌드가 `prototype-bus/index.html`과 `legal.html`을 함께 복사하는 구조임을 확인했다.
  현재 개인정보 안내가 익명 방문·설문 저장과 분석 도구 사용을 반영하지 않아 태그 연동과 함께 바로잡기로 했다.
- **GA4 태그 적용**: `index.html`과 `legal.html`의 `<head>`에 Google 태그를 추가했다.
  개인정보 안내에는 무작위 방문자 ID, 방문·설문 저장, GA4 처리 항목과 Google 정책 링크를 반영했다.

## 2026-07-29 (모집 준비)

- **22:20** 팀원 모집용 소개 카드 8종 제작. 당근 채용 공고 구조를 참고했으나 논리는 다르게 잡았다.
  당근은 아는 회사에 월급이 있고 여기는 둘 다 없다. 그래서 비전이 아니라 이미 굴러가는 증거를 앞세운다.
  - 조건은 무보수 사이드, 지분 없음으로 확정. 카드에 그대로 적었다. 숨기면 첫 대화에서 깨진다
  - 모집 글은 노션에 직접 작성하기로 함. 사이트에 페이지를 만들지 않는다
  - 카드 구성: 문제, 도착 좌석 예보, 대기 인원, 검열(기술적 핵심), 증거 수치, 사용자 흐름, 프론트 모집, 백엔드 모집
  - 아이폰 프레임은 CSS로만 그렸다. 베젤, 다이내믹 아일랜드, 반사광, 원근까지
  - **화면 목업은 실제 앱 CSS를 옮겨 썼다.** 값도 실제 관측(판교역 41% 만석, 줄 43명 이상)에 맞췄다.
    목업이 실물과 다르면 합류한 사람이 첫날 실망한다
  - 렌더는 Chrome 헤드리스 `--screenshot`, 2배 스케일로 2400x3000
- **23:10** 카드 2종 추가. 협업 보드(09)와 수식 카드(10).
  - 보드는 피그잼 형식이되 없는 팀원 이름을 박지 않았다. 커서를 모집 중으로 두어 빈 자리를 드러낸다.
    스티키는 실제로 열려 있는 과제(λ 시간대 분리, 낮 버킷 μ 0, JSONL을 DB로, 호출 예산)를 적었다
  - 수식 카드는 algebrica 스타일. `queue-recovery.md`의 λ 식별식과 대기 복원식,
    `boarding-model-v2.md`의 검열 조건과 좌석 전파식 네 개를 그림과 함께 실었다
  - 첫 렌더에서 검열 그림의 회색 영역이 x축 밖으로 빠지고 승차 계단이 도착선을 넘었다.
    수식이 근거인 카드라 그림이 틀리면 안 되므로 좌표를 다시 잡았다
- **23:35** 담백한 세트 3종 추가. 우테코 크루가 볼 예정이라 데모인 걸 아는 독자용이다.
  아이폰 프레임, 그라데이션, 원근을 빼고 화면을 스크린샷처럼 그대로 붙였다.
  과하게 다듬은 마케팅 컷은 뭘 가리나 하는 인상을 준다.
  - plain-01 배포된 화면 3종, plain-02 지금까지 나온 수치 8줄, plain-03 지금 열려 있는 것
  - 카드 높이를 내용에 맞췄다. 문서형이라 여백이 뜨면 미완성으로 읽힌다
