# 작업 기록

## 2026-08-05

- 사용자가 저장소 구조까지 포함한 전면 개편을 선택했다.
- 코드 의존성, 정적 사이트 조립, Vercel 함수, GitHub Actions의 경로 참조를 조사했다.
- 문서별로 독자, 역할, 현행성, 중복, 충돌을 조사했다.
- 변경 전 기준선으로 타입 검사, 47개 테스트, 호출 예산 계산, 재방문 집계 자체검사를 통과했다.
- 이전 `task_plan.md`, `findings.md`, `progress.md`를 `docs/archive/worklogs/`에 보관했다.
- 이번 개편을 위한 계획과 조사 기록을 새로 만들었다.
- 웹, API 구현, 도메인, 수집기, 백테스트, 보조 스크립트를 `apps`, `packages`, `tools`, `research` 아래로 이동했다.
- Vercel 진입점으로 쓸 루트 `api/`는 비워 두고 새 어댑터를 작성할 준비를 마쳤다.
- 새 디렉터리 깊이에 맞춰 모듈 import, 저장소 루트 계산, 기본 데이터 경로를 수정했다.
- 루트 `api/live.ts`, `api/feedback.ts`를 실제 핸들러를 내보내는 얇은 Vercel 어댑터로 만들었다.
- npm 명령, TypeScript 대상, ignore 규칙, 웹 모듈 경로를 새 구조에 맞췄다.
- 정적 사이트 조립을 `apps/web/scripts/assemble-site.mjs`로 분리해 Pages와 Vercel이 같은 구현을 쓰도록 만들었다.
- 사이트 루트를 기본 화면으로 바꾸고 기존 `/prototype-bus/` 주소에는 호환 리다이렉트를 남겼다.
- 수집과 발행 워크플로의 컴파일 산출물 경로를 새 구조에 맞추고, Pages의 중복 조립 절차를 공용 스크립트 호출로 교체했다.
- 옛 디렉터리의 JavaScript가 `dist/`에 남아 테스트가 중복 실행되는 일을 막기 위해 빌드 전 출력 디렉터리를 명시적으로 정리하게 했다.
- 로컬 HTTP 확인에서 `curl` 부재와 zsh 예약 변수 충돌을 확인해 Node 내장 `fetch` 방식으로 변경했다.
- 웹 데이터 생성기는 새 출력 경로를 도움말에 표시했다. 백테스트, 대기 복원, 라이브 채점은 `--help`를 지원하지 않고 비공개 원천 데이터도 없어 전체 실행을 생략했다.
- Phase 2 경로 개편을 마쳤다. 타입 검사, 47개 테스트, 호출 예산, 재방문 자체검사, 정적 사이트 HTTP 응답을 확인했고 기존 화면 데이터 네 파일의 해시는 그대로였다.
- 기존 문서를 `product`, `model`, `design`, `research`, `operations`, `archive`로 이동했다. 날짜가 고정된 실험 결과와 폐기된 v1은 현재 기준 문서와 분리했다.
- 루트 README를 새 실행 구조와 검증 범위에 맞춰 다시 쓰고, 문서 안내를 독자별 읽기 경로로 바꿨다.
- 제품 개요와 문제 검증을 새로 작성해 현장 계수, 좌석 관측, 인터뷰, 설문의 증거 범위와 편향을 구분했다.
- 기존 장문 모델 입문서는 보관하고, 현재 문서를 읽는 데 필요한 검열, 식별, 구간합, 평가 개념만 새 안내서로 정리했다.
- 구경로 검색식의 백틱이 셸에서 명령 치환으로 해석된 실패를 기록하고 검색 인용 방식을 수정했다.
- v2는 목표 설계, 시장 규모, 좌석 검증, 대기열 복원은 날짜가 고정된 연구 기록, v1과 옛 기획서, 입문서는 보관 문서라는 상태를 각 문서 첫머리에 명시했다.
- 코드 주석, GitHub Actions 요약, 디자인 카드가 가리키는 문서와 소스 경로를 새 구조에 맞춰 갱신했다.
- 설계와 연구 문서 안의 옛 파일 경로를 새 문서 및 코드 위치로 일괄 교체했다.
- 794줄 v2 원문을 보관 문서로 옮기고, 현재 구현과 미구현 목표를 분리한 새 설계안을 작성했다.
- Markdown의 로컬 링크를 전수 확인하는 `npm run check:docs` 명령을 추가했다.
- 현재 구현을 기준으로 좌석 예보, 탑승 판정, 줄 추정 문서를 각각 작성했다. 입력/계산/출력, 검증 근거, 미구현 범위와 코드 위치를 한 문서 안에서 확인할 수 있게 했다.
- 수집, 발행, 브라우저, API의 실행 흐름과 데이터 경계를 `docs/architecture.md`에 정리하고, 웹과 수집기 README는 해당 모듈의 실행법과 책임만 남겼다.
- 첫 문서 링크 검사에서 보관된 제안서의 옛 상대경로 3개가 깨진 것을 확인했다. 시장 규모와 좌석 검증은 새 연구 경로로, 당시 모델 설계는 보관한 원문으로 연결했다.
- 배포, 자동 수집, 예보 검증을 각각 독립 운영 가이드로 다시 쓰고 2026-07-27 토큰 만료와 2026-07-29 낡은 배포 사건을 날짜가 고정된 장애 기록으로 분리했다.
- 운영 문서는 외부 설정이 여전히 유효하다고 단정하지 않고, 대시보드, 응답, 워크플로, 데이터 시각으로 현재 상태를 확인하도록 바꿨다.
- 문서 링크 검사와 구경로 검색을 다시 실행해 보관 문서 밖의 옛 문서명이 남지 않았음을 확인했다.
- Phase 3 문서 정보 구조 개편을 마치고 Phase 4 문체 정리와 의미 보존 검증으로 넘어갔다.
- `humanize-korean` 준비 스크립트가 상대 실행 폴더를 플러그인 루트에서 찾는 차이를 확인했다. 입력 파일은 보존했고 이후 호출은 저장소의 절대경로를 사용한다.
- 도구 차이 기록을 한 번에 추가하려던 패치가 실제 절 제목과 맞지 않아 취소됐다. 파일을 다시 읽은 뒤 확인된 마지막 문맥을 기준으로 기록했다.
- 루트 README와 문제 검증 문서를 각각 `humanize-korean` 사전 점수로 검사했다. 두 문서 모두 light 경로가 선택돼 전면 재작성 대신 보수 윤문 대상으로 확정했다.
- 문제 검증 문서의 윤문 에이전트는 동시 작업 한도 때문에 바로 시작하지 못했다. 진행 중인 일관성 감사를 유지하고 자리가 생기는 즉시 완료된 에이전트를 재사용한다.
- 루트 README의 light 윤문과 구조 게이트를 마쳤다. 문자 변경률은 0.0%, 문장 터치율은 3.6%, 자체검증은 6/6이었고 연결어미 뒤 쉼표 4곳만 원문에 반영했다.
- 문서와 코드를 대조해 좌석 학습의 검열 조건, 공개 화면 데이터 범위, 상류 추천 규칙이 부정확하거나 빠진 곳을 찾았다.
- 현재 학습은 0석 경계만 수치 학습에서 제외한다는 점, 공개 `latest.js`에 식별자를 뺀 최신 차량 상태와 정류장 좌표가 들어간다는 점을 문서에 바로잡았다.
- 과거 만석 비율의 표본과 임계값, 강한 `가세요` 출력을 README, 제품 개요, 판정 문서, 목표 설계에 명시하고 이를 개인 탑승 확률로 오해하지 않도록 경계를 적었다.
- 문제 검증 문서의 light 윤문과 구조 게이트를 마쳤다. 문자 변경률은 0.1%, 문장 터치율은 5.0%, 자체검증은 6/6이었고 직접 인용을 제외한 연결어미 뒤 쉼표 8곳만 반영했다.
- 시간 버킷, 만석 연속, 현장 하한을 한꺼번에 바로잡으려던 패치가 겹쳐 읽은 경계 문장을 실제 중복으로 오인한 문맥 때문에 취소됐다. 변경은 적용되지 않았고 이후 수정은 파일별로 나눈다.
- README와 아키텍처에 화면, 발행 예측 로그, rolling-origin 백테스트의 시간 버킷 선택 차이를 적고, 실시간 API가 화면 데이터 생성 시점의 만석 연속 수를 갱신하지 않는다는 경계를 명시했다.
- 좌석 예보 문서에서 화면, 발행 예측 로그, rolling-origin 백테스트의 버킷 선택을 각각 설명했다. 고정된 과거 MAE가 정류장당 2분이라는 화면 가정까지 검증하지는 않는다는 한계도 추가했다.
- 탑승 판정 문서에 만석 연속 수가 `latest.js` 생성 시점의 정적 값이며 실시간 API로 갱신되지 않는다는 점과 관련 코드 위치를 추가했다.
- 최종 코드 대조에서 `apps/web/src/app.ts`에 추천, 이동 시간, 해소 추론 규칙이 UI와 함께 남은 구조 부채를 확인했다. Phase 4를 잠시 멈추고 순수 휴리스틱을 도메인 모듈과 테스트로 옮기는 Phase 2 보완 작업을 시작한다.
- 상류 추천 규칙을 `packages/domain/src/recommendation.ts`로 옮기고 임계값, 표본, 미정차, 후보 순서 경계 테스트를 추가했다. 화면은 현재 시간 버킷과 선택 상태만 주입하며, `boardingProbability`라는 이름은 실제 의미인 `nonFullObservationRate`로 바꿨다.
- 대기열 해소 감지, 하류 차량 기반 해소 추론, 최신 근거 선택, 60분 제한을 `packages/domain/src/queue.ts`로 옮기고 경계 테스트를 추가했다. 정류장당 2분과 서울 시간 버킷 계산은 `travel-time.ts` 한 곳에서 공유한다.
- `app.ts`는 세션 동안의 해소 시각 Map과 현재 시각만 관리하고 도메인 함수에 차량 상태를 넘기도록 바꿨다. 기존 동률 시 위치 추론을 택하는 규칙도 그대로 유지했다.
- 좌석 분포 전파, 정류장당 2분 버킷 선택, 탑승 판정 조립을 새 `packages/domain/src/forecast.ts`로 옮기고 현재 정류장 선반영, 버킷 경계, 미정차 전파, 만석 연속 테스트를 추가했다.
- 화면의 `forecastVehicle`은 브라우저에서 프로파일과 현재 시각을 가져와 도메인 예보 결과를 정류장 Map으로 바꾸는 얇은 어댑터가 됐다.
- 대기열 통합 뒤 타입 검사에서 표시 함수의 `ReturnType`이 옛 함수 이름을 가리키는 한 곳을 찾아 새 이름으로 수정했다.
- 문제 검증 문서에서 현장 잔류를 `29명 이상`으로 고치고, 최대 발송 대수 문항의 14응답과 1미상, 설문 표본의 해석 범위를 명시했다.
- 발행 예측 로그 생성기의 중복 좌석 분포와 시간 버킷 계산을 제거하고 화면과 같은 `forecastVehicleStops` 도메인 함수를 사용하도록 통합했다.
- 도메인 예보 통합 뒤 타입 검사와 전체 73개 테스트를 통과했다. Phase 2 보완을 마치고 문서의 수치와 표현 일관성을 정리하는 Phase 4로 돌아갔다.
- 아키텍처 문서에 `forecast`, `recommendation`, `queue`, `travel-time`의 책임과 화면, 발행 로그, 백테스트의 공유 경계를 반영했다.
- 운행 분할 조건이 코드의 엄격한 `>` 비교와 맞도록 프로파일 주석을 `45분 초과`로 바로잡았다.
- 수집 시간표 주석의 폐기된 `RUNNING.md` 참조를 현재 대기열 복원 연구와 좌석 예보 검증 근거로 바꿨다.
- 제품 개요에서 현장 잔류를 하한으로 고치고, 문제 빈도에 대한 판단을 관측 기간과 편의 표본 범위로 한정했다.
- 개념 문서에서 실제 차량 정원 `C`와 구현의 고정 80석 상태 상한을 구분하고, 새 예보, 추천, 이동 시간 모듈과 확장된 대기열 모듈을 코드 지도에 연결했다.
- 탑승 판정 문서에 `netDemandAt`의 인접 버킷 혼합 조건을 밝히고, 만차 뒤 대기 인원을 가능성으로 표현했으며 예보 필드명을 `seatAvailableProbability`로 맞췄다.
- 좌석 예보 문서에서 45분 초과 운행 분할, 하류 관측 기준 프로파일 귀속, 인접 버킷 혼합과 80석 계산 상한을 구분하고 예보 필드명과 코드 위치를 현재 구현에 맞췄다.
- 대기 범위 문서에서 `±35%`를 근거 없는 임시 표시 폭으로 분류하고, 16명 사례의 무하차 가정과 두 현장 사례의 일반화 한계를 명시했으며 해소 추론 코드 위치를 현재 구조에 맞췄다.

## 2026-08-06

- 편의 표본의 타깃 사용자 판단과 판정과 대기열 검증 표현을 과도한 확신 없이 관측 범위에 맞게 다듬었다. 문서 작업자의 최종 보고 실패와 직접 대조한 후속 조치도 계획에 기록했다.
- 예측 로그 생성기의 도움말과 주석이 화면 전체 판정을 기록한다고 오해되지 않도록 1, 3, 6정류장 좌석 예측이라는 실제 범위로 고쳤다.
- 아키텍처 문서에 발행 로그와 화면이 공유하는 계산, 서로 다른 기준 시각, 미정차 필터, 탑승 판정 범위를 구분했다.
- 좌석 예보 문서에서 라이브 채점이 발행 시각의 좌석 값만 평가하며 화면의 최종 판정 정확도는 측정하지 않는다는 한계를 명시했다.
- 예보 검증 운영 문서의 예측 파일 설명과 성적표 범위를 실제 로그 스키마에 맞춰 보완했다.
- 예측과 대기열 회귀 감사에서 도메인 계산 경계는 유지됨을 확인했다. 기존 발행 로그와 화면의 출력 범위 차이는 동작을 바꾸지 않고 조사 기록과 현재 문서에 남겼다.
- 구현과 미구현 범위, 날짜와 표본, 운영 상태와 장애 기록, 주요 문서 윤문 및 수치 보존을 다시 대조해 Phase 4를 마쳤다.
- 정적 사이트 조립 검증에서 새 도메인 테스트 JavaScript 네 파일도 복사되는 문제를 발견해 `*.test.js`를 공개 산출물에서 제외했다.
- 문서 링크, 타입 검사, 재방문 자체검사, 전체 73개 테스트와 호출 예산을 통과했다. 최대 사용일은 수요일 924회로 일 한도에서 76회가 남는다.
- 공개 데이터 네 파일의 해시가 개편 전과 같음을 확인했다. 정적 사이트에는 비밀값과 테스트 코드가 없고, 새 도메인 모듈과 기존 `/prototype-bus/` 호환 주소까지 로컬 HTTP로 확인했다.
- 보관했던 모델 수식 입문서를 `docs/model/mathematical-primer.md`로 복원했다. 문서 안내와 루트 README에서 현행 모델 참고서로 연결하고, 내부의 옛 문서 번호와 코드 경로는 현재 구조에 맞췄다.
