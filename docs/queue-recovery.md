# 대기 인원 복원: 좌석 관측만으로 정류장 줄 길이를 재구성하는 방법

작성일: 2026-07-26 · 상태: 방법 제안 + 단일 사례 실증 (확정 결론 아님)
관련 문서: `docs/validation-2026-07-24.md`(층-1 검증), `docs/boarding-model-v2.md`(v2 설계 방향)
대상: 경기도 직행좌석버스 3330, 2026-07-20~24 평일 수집분

## 0. 요약

`task_plan.md`는 "몇 명 대기" 예측기를 비산출로 규정했다. 근거는 체계적 대기 수집이 불가능하다는 것이었다. 이 문서는 그 전제가 틀렸음을 보인다. **대기 인원은 좌석 관측만으로 복원 가능한 양이다.**

핵심은 이미 데이터 안에 있던 관측 하나다. 직행좌석버스는 입석이 없으므로, **버스가 좌석을 남기고 출발했다는 것은 그 순간 대기 줄이 비었다는 뜻**이다. 이 사건이 큐 길이 0의 관측점이 되고, 두 관측점 사이에서 보존 법칙이 도착률을 식별해 준다.

2026-07-24 저녁 판교역에 적용한 결과, 필드 기록을 전혀 쓰지 않고 좌석 데이터만으로 18:57 시점 대기를 36~55명으로 추정했다. 같은 시각 현장 실측은 38명 이상이었다.

다만 5일치로 넓힌 검증(§7)에서 도착률 `λ`는 아직 예측기로 쓸 만큼 안정적이지 않았다. 한 날에서 뽑은 `λ`를 다른 날에 적용하면 3배 넘게 빗나간다. 현 단계의 산출은 **예측이 아니라 사후 재구성**이다.

분산의 원인은 둘로 갈린다(§8). 상류 관측으로 대신 읽은 버스의 오염을 걷어내면 40분 이상 세 구간이 λ 1.19~1.42로 수렴한다(변동계수 0.33 → 0.07). 남은 변동은 10~20분 구간의 표본 노이즈이며 이쪽은 수집 해상도가 담당한다.

## 1. 문제: 좌석 관측은 수요가 아니라 공급의 그림자를 본다

층-1 모형의 순수요 `μ`는 좌석 차분에서 학습된다. 그런데 좌석은 정원을 넘어 줄어들 수 없으므로, `μ`는 **실제 수요가 아니라 실현 승차**를 추정한다. 두 값은 혼잡하지 않을 때만 같다.

2026-07-24 저녁 판교역(seq22, 낙생육교)이 이 차이를 정량화했다.

| 항목 | 값 |
|---|---|
| 모형 추정 순수요 (역산·도착귀속, bucket 37) | 7.67명 |
| 실현 승차 (현장 실측) | 9명 |
| 실제 대기 (현장 실측) | 38명 이상 |

모형은 **탄 사람 수를 맞혔다**(오차 1.3명). 그러나 이용자가 답을 원하는 값은 9가 아니라 38이었다. 검증 보고서 §8-6이 한계로만 적어 둔 만석 검열이 여기서 처음 크기를 얻는다. 검열 배율은 하한 4.22배다.

원인은 두 겹이다.

**표현형.** 좌석 분포 전파는 수요 적용 시 0에서 잘린다.

```ts
// shared/profile.ts:220
const after = Math.min(capacity, Math.max(0, seats - demand));
```

20석 버스에 수요 38이 오면 `20 − 38 = −18`이 0으로 클램프되고 초과분 18은 소멸한다. `P(만석)`은 "자리가 없다"까지만 말할 수 있고 "얼마나 모자라다"를 담을 칸이 없다.

**학습 표본.** 만석이 낀 관측쌍은 수요가 은닉되므로 프로파일 학습에서 제외된다(`profile.ts:98`, `106`). 판교역 seq22 bucket 37(18:30~19:00)의 평일 학습분은 유효 표본 1.00에 검열 표본 16.00으로, **94%가 버려졌다.** 저녁 전체(17~20시)로 넓혀도 검열 비중은 68.4%다.

즉 초과 수요는 표현할 수도 없고, 표현할 수 있었어도 학습 데이터에서 먼저 지워진다.

## 2. 관측 가능한 큐 신호

직행좌석버스는 입석을 받지 않는다. 따라서 다음이 성립한다.

> 버스가 정류장 k를 **좌석을 남긴 채** 출발했다 ⟹ 그 순간 정류장 k의 대기 줄은 비어 있었다.

타려는 사람이 남아 있었다면 빈자리가 남을 이유가 없기 때문이다. 이 사건을 **큐 해소 이벤트**라 부른다.

판정은 하류 관측으로 한다. 하차가 무시할 만한 구간에서는 좌석이 진행 방향으로 단조 감소하므로, 정류장 k보다 하류인 어느 관측에서 잔여석이 0보다 크면 k에서의 출발 잔여석도 0보다 컸다. 특히 k 바로 뒤가 경유지(승차 없음)라면 그 관측이 곧 k의 출발 상태다.

판교역(seq22)은 뒤에 판교TG·청계터널 경유지가 이어져 이 조건이 깨끗하게 성립한다. 2026-07-20~24 저녁 17~20시에 판교역을 통과한 차량 112대 중 출발 잔여석이 관측된 건에서 **큐 해소 이벤트 38건**이 확인됐다.

## 3. 보존 논리와 도착률 식별

큐 길이가 0인 두 시점 `t₀`, `t₁` 사이에서는 줄에 들어온 사람과 버스로 빠져나간 사람이 정확히 같다. 남거나 모자란 몫이 양 끝에 없기 때문이다.

```
Λ(t₀, t₁) = Σ 승차(t₀, t₁]
```

`Λ`는 관측할 수 없고 `Σ 승차`는 좌석 차분으로 관측된다. 따라서 관측 불가능한 도착량을 관측 가능한 승차량으로 대신 셀 수 있다. 구간 평균 도착률은 다음과 같다.

```
λ = Σ 승차(t₀, t₁] / (t₁ − t₀)
```

**경계 조건이 방법의 전부다.** `t₀`와 `t₁`는 **둘 다 큐 해소 시점**이어야 한다. 혼잡이 시작된 시점은 기준점으로 쓸 수 없다. 그 순간 큐가 0이었다는 보장이 없기 때문이다.

```
t₀ 해소 ─────────────── 혼잡 구간 ─────────────── t₁ 해소
 큐=0                                              큐=0
       ↑ 어딘가에서 막히기 시작하지만 그 시점은 알 필요가 없고,
         알아도 기준점으로 쓸 수 없다
```

혼잡 시작점부터 재면 분모만 작아져 `λ`가 부풀어 오른다.

## 4. 누적곡선과 대기 길이

`λ`는 **분당 줄에 합류하는 인원**이지 줄 길이가 아니다. 줄 길이는 도착과 승차의 누적 차이로 나온다.

```
대기(t) = λ · (t − t₀) − Σ 승차(t₀, t]
```

도착은 기울기 `λ`의 직선으로, 승차는 버스가 올 때만 뛰는 계단으로 쌓인다. 두 곡선의 세로 간격이 그 순간 서 있는 사람 수다.

```
누적 인원
   ↑                                    ╱ 도착 (기울기 λ)
   │                                 ╱
   │                              ╱ ┊
   │                           ╱    ┊ ← 세로 간격 = 그 순간 대기 인원
   │                        ╱  ┏━━━━┛
   │                     ╱  ┏━━┛
   │                  ╱  ┏━━┛      승차 (버스 도착 시에만 상승)
   │               ╱ ┏━━━┛
   │            ╱┏━━━┛
   │         ╱┏━┛
   └────────┴──────────────────────────┴─────────→ 시간
           t₀                          t₁
         큐 해소                      큐 해소
```

두 곡선이 만나는 지점이 큐 해소 시점이다. 교통류 이론의 누적곡선(cumulative curve) 방법과 같은 구조다.

## 5. 실증: 2026-07-24 저녁 판교역

3330 안양 방면, 판교역.낙생육교.현대백화점(seq22). 차량 식별자는 데이터 경계 원칙에 따라 기재하지 않는다.

| 관측 시각 | 도착 잔여석 | 출발 잔여석 | 승차 | 큐 해소 |
|---|---|---|---|---|
| 17:40 | 35 | 5 | 30 | **예** |
| 18:01 | 0 | 0 | 0 | 아니오 |
| 18:01 | 10 | 0 | 10 | 아니오 |
| 18:01 | 0 | 0 | 0 | 아니오 |
| 18:11 | 24 | 0 | 24 | 아니오 |
| 18:21 | 1 | 0 | 1 | 아니오 |
| 18:31 | 0 | 0 | 0 | 아니오 |
| 18:31 | 8 | 0 | 8 | 아니오 |
| 18:31 | 1 | 0 | 1 | 아니오 |
| 18:42 | 2 | 0 | 2 | 아니오 |
| 18:42 | 6 | 0 | 6 | 아니오 |
| 18:52 | 0 | 0 | 0 | 아니오 |
| 18:57 | 9 | 0 | (미관측) | 아니오 |
| 19:02 | 27 | 21 | 6 | **예** |
| 19:02 | 44 | 8 | 36 | **예** |

17:40 해소 이후 **82분 동안 좌석을 남기고 떠난 버스가 한 대도 없다.** 다음 해소는 19:02다. 구간 내 관측된 총 승차는 94명이다.

18:57 통과 차량은 10분 샘플링 사이에 빠져 승차량이 관측되지 않았다. 이 값을 미지수 `b`로 두고 범위를 잡는다. 상한은 그 차량이 상류에서 가지고 있던 잔여석 20이다.

| `b` | λ | 18:57 예상 대기 |
|---|---|---|
| 0 (하한) | 1.15명/분 | **36.3명** |
| 20 (상한) | 1.39명/분 | **55.0명** |

**현장 실측: 38명 이상** (기록자가 줄에 서서 직접 계수, 확실한 하한).

필드 기록을 계산에 전혀 넣지 않고 좌석 데이터만으로 얻은 구간이 실측 하한을 포함한다. 참고로 필드 실측값 `b = 9`를 넣으면 λ = 1.26명/분, 예상 대기 44.7명이다.

## 6. 한계

1. **`λ` 일정 가정.** 82분을 단일 도착률로 뭉갠다. 피크 안에서 도착률이 오르내린 것은 반영되지 않는다. 18:57 추정이 실측 하한보다 높게 나온 원인 중 하나로 추정된다.
2. **상류 오염.** 도착 잔여석을 정류장 k에서 직접 관측하지 못한 경우 상류 관측으로 대신했고, 그 승차량에는 상류 정류장 승차가 섞인다. `λ`를 위로 편향시킨다.
3. **단일 사례.** 필드 실측과 대조된 검증 사례가 1건이다. `λ`의 날짜 간 안정성은 §7에서 별도로 측정했고, 결과는 부정적이다.
4. **10분 샘플링.** 판교역 저녁 통과 차량 중 승차량을 직접 산출할 수 있었던 비율은 44%다. 18:57 차량처럼 통째로 빠지는 경우가 생긴다.
5. **노선별 큐.** "좌석 남기고 출발"은 해당 노선 대기자만 센다. 같은 정류장에서 다른 노선을 기다리던 인원은 포함되지 않는다. 제품이 답할 질문("이 노선을 탈 수 있는가")에는 이 정의가 맞지만, 정류장 총 혼잡도와는 다른 값이다.
6. **하차 가정.** 대상 구간에서 하차를 0으로 본다. 저녁 판교역은 평일 17~20시 통과 관측쌍 104건 전부에서 잔여석이 감소·유지였다. 다만 감소는 승차−하차의 합성이므로 순수 하차 0의 증명은 아니다.
7. **혼잡 구간이 길수록 해상도가 떨어진다.** 저녁 내내 해소가 없는 정류장에서는 `λ`를 식별할 수 없다.

## 7. λ 안정성 검증 (2026-07-26)

§6-3의 단일 사례 한계를 좁히기 위해 판교역 5일치(2026-07-20~24 평일, 저녁 17~21시)에 방법을 적용했다.

**구현 주의.** 하류 판정에는 경유지만 써야 한다. 초기 구현이 `의왕청계영업소`(seq25)를 경유지로 오인해 포함했더니 하차분이 섞여 도착률이 음수로 나오는 구간이 생겼다. 판정 범위를 실제 경유지(seq23·24)로 좁히자 사라졌다. 정류장 이름의 `(경유)` 표기가 유일한 판별자다.

### 결과

산출 구간 17개.

| 지표 | 값 |
|---|---|
| 평균 λ | 1.41명/분 |
| 중앙값 | 1.18명/분 |
| 표준편차 | 0.85 |
| 범위 | 0.00 ~ 3.41 |
| 시간가중 평균 | 1.41명/분 (총 승차 618명 / 총 439분) |

버스 1~2대로 산출된 짧은 구간은 요율이라기보다 개별 버스의 승차량이므로, 40분 이상 구간만 추리면 다음과 같다.

| 날짜 | 구간 | 길이 | λ |
|---|---|---|---|
| 07/24 | 17:40~19:02 | 82분 | 1.14 |
| 07/22 | 19:21~20:12 | 51분 | 0.86 |
| 07/23 | 18:31~19:22 | 51분 | 2.12 |
| 07/21 | 18:51~19:32 | 41분 | 2.35 |
| 07/23 | 19:22~20:03 | 41분 | 0.64 |
| 07/20 | 19:52~20:33 | 41분 | 1.58 |

시간대별로 묶으면 저녁 피크(18:30~19:00)가 높고 이후 낮아지는 형태가 보이지만, 표본이 각 버킷당 1~6개이고 범위가 서로 겹친다.

| 버킷 | n | 평균 λ | 범위 |
|---|---|---|---|
| 18:00 | 1 | 1.14 | — |
| 18:30 | 1 | 2.12 | — |
| 19:00 | 4 | 1.91 | 0.00~3.41 |
| 19:30 | 6 | 1.08 | 0.64~1.96 |
| 20:00 | 4 | 1.39 | 0.00~2.10 |

### 판정: 아직 예측기로 쓸 수 없다

크기의 자릿수는 일관된다(1~2명/분). 그러나 긴 구간끼리도 0.64~2.35로 **3.7배** 벌어지고, 같은 시간 버킷 안에서도 범위가 겹친다.

결정적인 반증은 교차 적용이다. 7/21·7/23의 피크 구간 λ(2.12, 2.35 → 평균 2.24)를 7/24에 그대로 적용하면 18:57 예상 대기는 다음과 같다.

```
2.24 × 77분 − 52명 = 약 120명
```

현장 실측은 38명 이상이었다. **3배 넘게 빗나간다.** 7/24 구간이 17:40부터 시작해 피크 이전의 한산한 시간을 포함하는데 피크 λ를 통째로 곱했기 때문이다. §5의 추정이 맞았던 것은 그 구간의 λ를 그 구간 자체에서 뽑았기 때문이며, 다른 날에서 가져온 λ로는 재현되지 않는다.

즉 `λ`는 시간에 따라 변하는 양인데, 현재 데이터로는 그 변화를 분해할 만큼 큐 해소 이벤트가 촘촘하지 않다.

### 원인은 셋 다 샘플링 해상도다

1. **큐 해소가 드물다.** 5일간 판교역 저녁에서 얻은 산출 구간이 17개뿐이고, 그중 절반이 버스 1대짜리다. 구간이 길어질수록 λ의 시간 해상도가 떨어진다.
2. **상류 오염.** 정류장에서 도착 잔여석을 직접 못 읽어 상류 관측으로 대신한 버스가 대부분의 구간에 섞여 있다(예: 7/21 41분 구간은 4대 중 3대). 상류 정류장 승차가 판교역 몫으로 계산돼 λ를 위로 민다.
3. **정류장 확장이 막힌다.** 하류 경유지를 가져 출발 잔여석을 깨끗이 읽을 수 있는 정류장은 85개 중 7개이고, 그중 경유지 자신을 빼면 실제 승차 정류장은 5개다(판교역, 의왕청계영업소 2곳, 안양역, 농수산물시장). **아침 실측 지점인 범계역(seq55)은 포함되지 않는다.** 나머지 정류장에서는 연속 시퀀스 관측이 필요한데 10분 샘플링에서는 거의 잡히지 않는다.

## 8. 상류 오염 보정 (2026-07-26)

§7이 꼽은 원인 2를 제거하면 분산이 얼마나 줄어드는지 확인했다. 조밀 수집분을 기다리지 않고 기존 5일치로 답할 수 있는 질문이다.

### 보정 방법

정류장에서 도착 잔여석을 직접 못 읽은 버스는 상류 관측 `s`로 대신했고, 그 승차량에는 정류장 `s`~21의 승차가 섞여 있다. 역산·도착귀속 프로파일로 그 몫을 제거한다. 보정 대상 날짜는 프로파일 학습에서 뺀다(leave-one-day-out).

**기대 승차를 단순히 빼면 안 된다.** 첫 구현이 그렇게 했다가 7/24 18:57 버스에서 상류 기대치 28.9명이 그 버스의 잔여석 20석을 넘어 승차량이 0으로 잘렸다. 실제 버스는 자리가 없으면 그만큼만 태운다. 좌석 분포를 정류장마다 전파하면(`applyNetDemand`의 0 클램프) 이 제약이 지켜진다. §1이 지적한 클램프가 여기서는 결함이 아니라 필요한 성질로 쓰인다.

또한 관측 범위를 상류 seq18까지 넓혔다. §7은 seq20~22에서만 도착 잔여석을 찾아 그보다 위에서만 잡힌 버스를 통째로 버렸는데, 보존 논리는 구간 내 **모든** 승차를 세야 성립하므로 버스를 빼면 Σ승차가 과소 집계된다. 그 결과 산출 구간이 17개에서 30개로 늘었고, §7의 수치와는 표본이 다르다.

### 보정 자체의 검증

7/24 18:57 버스는 판교역 통과 순간이 샘플링에서 빠졌고 필드 실측으로 승차 9명이 확정된 유일한 사례다. 상류 seq19 관측(20석)에서 출발해 보정하면 판교역 도착 잔여석이 **7.8석**, 곧 승차 7.8명으로 나온다. 실측 9명 대비 **1.2명** 차이다. 보정하지 않으면 20명으로 두 배 넘게 과대 집계된다.

기대 승차를 단순히 빼는 변형은 이 사례에서 무너진다. 상류 기대치가 그 버스의 잔여석 20석을 넘어 승차량이 0으로 잘리기 때문이다. 커밋된 도구는 분포 전파만 구현한다.

### 결과

긴 구간에서 서로 다른 날의 λ가 수렴한다.

| 날짜 | 구간 | 길이 | 상류 대체 | λ 보정 전 | λ 보정 후 |
|---|---|---|---|---|---|
| 07/21 | 17:30~18:51 | 82분 | 16대 중 14대 | 3.25 | **1.42** |
| 07/24 | 17:40~19:02 | 82분 | 17대 중 8대 | 2.29 | **1.31** |
| 07/21 | 19:52~20:33 | 41분 | 2대 중 1대 | 1.40 | **1.19** |

| 표본 | 지표 | 보정 전 | 보정 후 |
|---|---|---|---|
| 40분 이상 (n=3) | 표준편차 | 0.75 | **0.09** |
| | 변동계수 | 0.33 | **0.07** |
| 전체 (n=30) | 표준편차 | 1.39 | 0.94 |
| | 변동계수 | 0.48 | 0.53 |

7/24 18:57 대기 재추정도 실측 쪽으로 크게 이동한다.

| | λ | 예상 대기 |
|---|---|---|
| 보정 전 | 2.29명/분 | 78.4명 |
| **보정 후** | **1.31명/분** | **44.5명** |
| 실측 | — | 38명 이상 |

다만 이 λ는 그 구간 자체에서 뽑은 값이므로 여전히 사후 재구성이다. 독립적으로 검증된 것은 보정 절차이지 예측력이 아니다.

### 판정: 두 분산의 원인이 다르다

- **긴 구간의 분산은 오염이 주범이었다.** 40분 이상 세 구간이 보정만으로 1.19~1.42 안에 모였다. 변동계수 0.33 → 0.07이다. 상류 대체 비율이 14/16과 8/17로 크게 달랐는데도 같은 자리에 수렴한 것이 특히 시사적이다.
- **짧은 구간의 분산은 오염이 아니다.** 전체 30구간의 변동계수는 오히려 나빠졌다(0.48 → 0.53). 표준편차는 줄었지만 평균이 더 크게 줄었기 때문이다. 10~20분 구간은 버스 1~2대의 승차량을 시간으로 나눈 값이라 애초에 요율이 아니라 개별 관측이며, 여기 남은 변동은 표본 노이즈다.

즉 오염 보정은 **계통 편향**을 걷어내고, 조밀 수집은 **표본 노이즈**를 줄인다. 서로 다른 문제를 서로 다른 수단이 담당하며, 둘 다 필요하다. n=2라는 점은 그대로 한계다.

## 9. 다음 단계

1. **수집 간격 축소 — 적용됨(2026-07-26).** 피크 창 안 핵심 시간대(평일 아침 07:00~09:00, 저녁 18:00~19:30)를 60초 간격으로 바꿨다(`collect-bus-seats.yml`, `RUNNING.md`). 배치 근거는 만석 분포 실측(아침 절정 07:00~08:00, 저녁 절정 18:00~18:30)과 필드 기록 시각(08:37, 18:57)이며 둘 다 구간 안에 들어온다. 전 피크를 60초로 돌리면 API가 한도에 육박해 재시도 한 번에 수집이 죽으므로 핵심 구간에만 적용했다. 평일 490콜/일(49%)이다. 60초면 대부분 정류장에서 도착 상태가 직접 잡혀 원인 1·2·3이 함께 줄고, 검증 보고서 §8-2가 남긴 귀속 규약 재확인도 같이 풀린다.
2. **λ 재검증.** 조밀 수집분이 쌓이면 같은 절차를 다시 돌려 버킷별 λ의 분산을 측정한다. §8이 계통 편향을 걷어냈으므로 남는 것은 표본 노이즈여야 한다. 이때도 긴 구간끼리 3배 이상 벌어지면 방법이 아니라 전제(입석 없음, 하차 무시)를 의심해야 한다.
3. **필드 앵커 추가.** 현재 검증 앵커가 7/24 판교역 1건뿐이다. 다른 정류장·다른 날의 대기 계수 기록이 들어오면 교차 적용 검증이 가능해진다.
4. **정류장 확장.** 판교역 말고도 출발 잔여석을 깨끗하게 읽을 수 있는 정류장이 넷 더 있다(§7 원인 3). 같은 도구로 확인할 수 있다.
5. **범위 재정의.** `task_plan.md`의 "몇 명 대기 예측기 = 비산출" 항목은 체계적 대기 수집이 불가능하다는 전제에 기대고 있었다. 좌석 관측만으로 복원 가능하다는 것이 확인됐으므로 전제가 바뀌었다. 다만 현재 해상도로는 예측기가 아니라 사후 재구성에 머무르므로, 승격은 1·2가 끝난 뒤가 맞다.

## 부록. 재현

이 문서의 수치는 `backtest/queue-recovery.ts`가 낸다. 수집 저장소를 클론한 뒤 다음을 돌린다.

```
npm run queue -- --data-dir=<수집 저장소>/data
```

기본값은 3330 판교역(seq22), 평일 저녁 17~21시, 상류 대체 seq18까지, 상류 오염 보정 켬이다. 절별 설정은 다음과 같다.

| 절 | 추가 인자 |
|---|---|
| §5 · §7 | `--no-correct --upstream-from=20` |
| §8 보정 전 | `--no-correct` |
| §8 보정 후 | (기본값) |

`--json=<경로>`를 주면 구간과 통과 이력이 그대로 저장된다. §5의 통과 이력 표와 §8의 18:57 검증은 그 파일의 `passes` 배열에서 나온다.

다른 정류장·시간대도 같은 도구로 본다. 뒤에 경유지가 없어 출발 잔여석을 깨끗하게 읽을 수 없는 정류장이면 도구가 먼저 그 사실을 알린다.

```
npm run queue -- --data-dir=... --stop=43 --from-hour=7 --to-hour=10
```

수집이 계속되므로 표본이 늘면 수치도 바뀐다. 위 값은 **2026-07-20~24 평일분** 기준이다.
