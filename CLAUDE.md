# bus-seat-tracker

## git

전역 규칙의 예외다. 이 저장소에서는 커밋과 푸시를 직접 실행해도 된다.

- 작업 브랜치(`feature/*`)에서만. `main`에는 직접 커밋·푸시하지 않고 PR로만 반영한다.
- `merge`, `rebase`, `push --force`, `reset --hard`는 여전히 금지 — 제안하고 대기한다.
- 커밋하기 전에 무엇을 어떤 커밋으로 나눌지 먼저 보고한다.
- 커밋 메시지는 최근 로그를 따라 한국어 제목을 쓴다. 본문은 쓰지 않는다.
- PR 본문과 커밋에 `Co-Authored-By`, `Generated with Claude Code` 푸터를 넣지 않는다.

## 데이터 경계

수집 스냅샷과 원본 차량 ID(HMAC)는 비공개 저장소 `bus-seat-tracker-data`에만 둔다.
공개 화면·공개 저장소에는 차량 번호, 원본 차량 ID, 원본 스냅샷을 내보내지 않는다.

## 호출 예산

공공데이터포털 일 한도 1,000회가 이 서비스의 구속 제약이다. 수집이 약 482회를 쓴다.
수집 주기·노선 추가·실시간 기능을 건드리기 전에 예산부터 계산한다 (`DEPLOY.md` §6).
